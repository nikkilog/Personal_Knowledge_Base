# 踩坑日志

> 用途：记录已经真实发生、容易复发、具有跨任务复用价值，并且根因与处理方式已经充分建立的故障经验。
>
> 不记录：普通个人偏好、项目当前状态、项目专属下一步、一次性 ID、Secret 值、大段原始日志或尚未验证的担忧。
>
> 状态：Current
> 最后更新：2026-07-26

## 1. 使用与维护规则

- 本文按问题类型组织，不按聊天日期堆叠流水账。
- 一个 Pitfall 对应一个核心根因。相同触发条件、相同根因和相同修复机制的表现应合并；能够独立发生、根因或验收不同的问题应拆分。
- 当前条目统一包含：状态、问题与适用范围、可观察表现、根因与常见错误处理、正确处理、验证与防复发。
- 状态只使用：
  - `已确认`：问题、根因和处理方式已经通过实际结果验证；
  - `部分确认`：问题已发生，但仍有部分根因或边界待复核；
  - `待验证`：当前主要是推测，不得作为正式通用结论；
  - `已废弃`：条目不再适用于当前环境，但保留历史说明；
  - `已替代`：内容已并入或由另一条目取代。
- 已分配的 `P` 编号永久保留，不因重排、合并或废弃而复用。合并后的旧编号保留简短重定向。
- 同一个问题再次出现时优先更新原条目；措辞不同不构成新条目。
- 平台行为可能变化。无法确认仍适用时先标记 `待验证`；被新机制替代时标记 `已替代` 并指出继任条目。
- 项目专属事实、版本、资源标识和业务选择规则应留在来源项目。只有删除项目名称后仍成立的触发条件、根因、处理和验收可以进入本文。
- 新增或大幅改写条目前，应检查是否已经由现有条目覆盖，并确认不会与 `PERSONAL_CONTEXT.md` 中的普通偏好重复。


## 2. Google 身份认证、Drive 与 Sheets

### P001｜Service Account 无法在 My Drive 创建文件

- **状态**：已确认
- **问题与适用范围**：Colab 使用 Service Account 在 Google Drive 中新建 Spreadsheet、备份文件或执行需要占用个人 Drive 配额的操作。
- **可观察表现**：`403 storageQuotaExceeded`；即使 Service Account 能读取或更新已共享文件，也可能无法创建新文件。
- **根因与常见错误处理**：把"拥有 API 权限"和"拥有可用 Drive 存储配额"当成了同一件事。Service Account 通常没有个人 My Drive 配额。
- **正确处理**：日常读取和更新已共享文件可继续使用 Service Account；需要新建、复制或备份时，使用用户 OAuth（`COLAB_USER`），或把目标放在允许 Service Account 操作的 Shared Drive。
- **验证与防复发**：使用目标身份实际创建一个测试文件，并确认文件位于预期文件夹且所有者/权限正确；不能只以认证成功作为验收。

### P002｜授权弹窗运行到中途才出现

- **状态**：已确认
- **问题与适用范围**：Notebook 已完成大量下载或计算后，写入阶段才触发 Google OAuth。
- **可观察表现**：用户以为流程可以无人值守，运行到一半突然要求授权；错过弹窗后任务中断或超时。
- **根因与常见错误处理**：认证被藏在业务流程内部，没有在运行开始时完成。
- **正确处理**：Cell 1 只放 Controller；如果必须 OAuth，授权 Cell 必须紧接 Cell 1。之后所有 Cell 复用已建立的凭据，不再次触发交互授权。
- **验证与防复发**：从全新 Runtime 自上而下运行，确认所有必要弹窗都在 Cell 1 后立即出现，后续长任务不再要求人工介入。

### P003｜把 httplib2 的 timeout 警告当成任务失败

- **状态**：已确认
- **问题与适用范围**：使用 Google 官方认证/客户端库访问 Drive 或 Sheets。
- **可观察表现**：出现 `httplib2 transport does not support per-request timeout`，但后续仍能看到扫描心跳、读取进度或正常结果。
- **根因与常见错误处理**：仅凭 WARNING 判断任务已经失败，或者反过来完全忽略真正的超时风险。
- **正确处理**：先看是否持续输出心跳、行数和当前处理对象。该 WARNING 本身不等于失败；若确实需要超时控制，应在构造底层 `httplib2.Http` 时统一设置，而不是给单次请求传不支持的 timeout。
- **验证与防复发**：确认请求继续推进并最终输出 `[SUMMARY]`；若修改了 timeout，使用可控的慢请求验证确实会按设定中止。

### P004｜目标工作表缺失或写入范围超过 Google Sheet 网格

- **状态**：已确认
- **问题与适用范围**：通过 gspread 或 Sheets API 批量写入比当前 Sheet 行列数更大的区域，或首次向尚不存在的目标工作表写入，包括按已有数据计算下一行并追加日志。
- **可观察表现**：目标工作表不存在时报错；或出现 HTTP 400，常见文本为 `Range exceeds grid limits`；动态区块增加时，也可能在本可扩容的 `columnCount` 检查处提前失败。
- **根因与常见错误处理**：假设目标工作表及默认网格已经存在且足够；只按 DataFrame 生成写入 Range，或按 `len(existing) + 1` 计算 `start_row` 后直接 `worksheet.update()`，没有在写入前检查目标存在性，以及结束行列是否超过 Sheet 的 `rowCount` 与 `columnCount`；也可能把 capacity validation 放在 resize 之前。
- **正确处理**：写入 Adapter 先获取目标工作表，不存在时按正式规则自动创建；动态布局遵循 `calculate → resize → validate`：先计算包含表头、全部动态区块或本次追加批次的所需结束行列数；不足时用 `resize()`、`add_rows()` 或 `batchUpdate` 先扩展网格，扩容应留出合理余量，再校验容量并分块执行 values update。若是全量替换，先清空目标区域但不要误删 Sheet。本地数据应先成功落盘；线上同步失败必须明确记录，但不得损坏或回滚已成功写入的本地 Source of Truth。
- **验证与防复发**：读取 Sheet properties，分别在目标工作表不存在、行数不足和列数不足时验证自动创建与扩容；确认网格尺寸不小于计划写入范围，写入后核对本地行数、线上行数、实际行列数、最后一个目标单元格和同步状态，并连续追加多批确认不会在下一个边界立即复发。

### P005｜写完以后回读比对全部 mismatch

- **状态**：已废弃（资格不足）
- **说明**：相关 mismatch 现象曾发生，但原记录没有建立唯一根因，处理方式也未完成可重复验证，因此不作为正式通用 Pitfall。P005 编号永久保留，不再复用；如以后取得充分证据，应在本编号原位重建完整条目。

### P006｜默认新建 Tab，导致同类输出越来越多

- **状态**：已确认
- **问题与适用范围**：每次运行都以时间戳或"new"名称创建新 Sheet Tab。
- **可观察表现**：Spreadsheet 中出现大量近似 Tab，引用关系混乱，用户无法判断哪个是正式结果。
- **根因与常见错误处理**：把避免覆盖风险等同于不断创建新目标，却没有稳定的正式输出入口。
- **正确处理**：正式 Tab 名固定；默认清空并全量写入同名 Tab。只有用户明确要求保留快照时才创建带时间版本的备份。
- **验证与防复发**：连续运行两次后，目标 Tab 数量不增加；正式 Tab 内容更新，引用仍指向同一位置。

  ## 3. Pandas、数据结构与汇总

### P032｜OAuth Client Secret 生命周期误判

- **状态**：已确认
- **问题与适用范围**：创建 Google OAuth Client 后，需要保存 client_secret 用于无弹窗认证。
- **可观察表现**：后续无法再次查看完整 secret，误认为凭据丢失。
- **根因与常见错误处理**：OAuth Client Secret 通常只在创建阶段完整展示。
- **正确处理**：创建后立即保存：
  - Client ID
  - Client Secret
  - JSON 文件

  如果遗失：
  - 创建新的凭据；
  - 不覆盖旧配置；
  - 新配置验证成功后再切换。
- **验证与防复发**：确认：
  - credentials 可以生成；
  - refresh token 可以工作；
  - API 可以访问。

### P033｜OAuth JSON 字段含义混淆

- **状态**：已确认
- **问题与适用范围**：读取 OAuth JSON 时，将 token_uri 与 refresh_token 混淆。
- **可观察表现**：无法建立无弹窗认证。
- **根因与常见错误处理**：token_uri：

  是 Google Token Endpoint 地址。

  refresh_token：

  是授权后的长期刷新凭据。

  两者用途不同。
- **正确处理**：明确保存：

  CLIENT_ID CLIENT_SECRET REFRESH_TOKEN

  使用 OAuth Library 管理流程。
- **验证与防复发**：credentials 可以刷新 access token，并成功调用 API。

### P042｜刷新既有 OAuth Token 时擅自扩大授权范围

- **状态**：已确认
- **问题与适用范围**：使用 Refresh Token 进行 Google OAuth 非交互式刷新时，刷新请求重新发送不同或扩大的 scopes；适用于复用既有离线授权的 Notebook、脚本和服务。
- **可观察表现**：凭据存在但刷新报 `RefreshError invalid_scope`；短重试仍重复失败。
- **根因与常见错误处理**：Refresh Token 绑定原授权范围，刷新逻辑却把当前代码期望的更大 scopes 当成可在刷新阶段追加的参数。

  常见错误处理：持续短重试，或在未判断根因时重新引入交互授权。
- **正确处理**：按原授权范围刷新，刷新阶段不擅自增加 scopes；确需扩大权限时，把重新授权作为独立、明确的用户授权流程。
- **验证与防复发**：使用原授权范围成功刷新 access token 并调用范围内 API；扩大权限的需求不会被刷新逻辑静默处理。

  防复发：离线刷新逻辑不得自行改变或扩大 Refresh Token 的授权范围。

### P043｜Secret 解析遗漏运行环境中的真实受控来源

- **状态**：已确认
- **问题与适用范围**：Notebook 或脚本依赖 Secret，但解析逻辑只检查单一来源或有限默认路径，没有覆盖当前运行环境实际使用的受控来源。
- **可观察表现**：代码报告 `client_id`、`client_secret`、`refresh_token` 全部缺失，实际 Secret 文件或变量已经存在。
- **根因与常见错误处理**：把某个 Runtime 的单一路径或 Secret 根目录当成完整事实，没有按明确优先级处理显式值、Colab userdata、环境变量、受控子目录文件、dotenv 别名或当前项目白名单路径。

  常见错误处理：直接判定 VS Code、Python Kernel 或 OAuth 凭据失效；在日志中输出 Secret 值以排查。
- **正确处理**：依次区分 Runtime、路径解析、Secret 名称映射和 Token 刷新。按固定且可说明的优先级读取当前项目显式允许的来源；受控文件发现应覆盖声明的子目录和别名，但不得扫描中央 Secrets 区域、猜测其他项目文件或混用相似名称。日志只打印命中的来源类型、已检查的非敏感路径或缺失名称，绝不打印 Secret 值。
- **验证与防复发**：分别对显式值、Colab userdata、环境变量、受控文件和 dotenv 别名执行加载测试；每次确认解析结果来自预期来源，日志只包含来源类型、非敏感路径或 Secret 名称，不包含值。

  防复发：Secret 解析必须覆盖各 Runtime 明确允许的多来源入口，同时保持固定优先级和项目白名单边界，不能退化为单一路径假设或跨项目目录扫描。

### P044｜Sheets 逐区块读取与短重试放大 429 配额错误

- **状态**：已确认
- **问题与适用范围**：审计 Google Sheets 宽表或重复区块时，对每个区块单独读取，并对配额错误进行连续短重试。
- **可观察表现**：请求按 1、2、4 秒退避后仍持续返回 429，任务长时间撞击每分钟读取配额。
- **根因与常见错误处理**：API 请求数量过多，且重试策略没有识别配额恢复所需的完整窗口。

  常见错误处理：保持逐区块读取并反复短重试，或把每次 429 当作独立瞬时网络错误。
- **正确处理**：优先使用 `batchGet` 合并读取，在本地切片和校验；识别为配额错误时等待完整配额窗口，并限制重试次数。
- **验证与防复发**：记录同一审计范围的请求数量，确认宽表读取由多次逐区块请求合并为批量请求；配额模拟测试不会在窗口内持续发请求。

  防复发：宽表布局检查不得默认逐区块读取，配额错误不得只采用秒级短重试。

### P059｜Google Sheet 未分享给当前 Service Account 导致 403

- **状态**：已确认
- **问题与适用范围**：使用 gspread、Sheets API 或 Drive API 读取、更新一个已经存在的 Google Sheet，而当前认证身份是 Service Account。
- **可观察表现**：返回 `403 The caller does not have permission`、`PermissionError` 或等价的文件访问拒绝；认证本身可能成功。
- **根因与常见错误处理**：目标表格没有分享给当前 Service Account，或分享给了另一个 Service Account。API 认证成功不代表目标文件已经授权。

  常见错误处理：反复修改代码、重新生成凭据或把问题当成 Sheet 网格、OAuth 或 API 语法错误。
- **正确处理**：确认当前实际使用的 Service Account 邮箱，并在目标 Google Sheet 的共享设置中授予所需权限；随后重新运行。只有授权仍正确但错误持续时，才继续检查文件 ID、身份加载和 API scope。
- **验证与防复发**：用当前 Service Account 对目标表执行一次最小读取；需要写入时再对测试单元格写入并回读。确认访问的是预期文件和预期身份。

  防复发：新建或更换目标 Sheet、Service Account 或凭据映射后，先完成一次最小权限验收。


## 3. 数据结构、Schema 与 API

### P007｜`TypeError: unhashable type: 'numpy.ndarray'`

- **状态**：已确认
- **问题与适用范围**：对包含 `numpy.ndarray`、list 或其他嵌套对象的 Series 执行 `factorize`、`Categorical`、排序、去重、分组或设为索引。
- **可观察表现**：Pandas 在 `factorize()` 或 `Categorical` 内部报 `TypeError: unhashable type: 'numpy.ndarray'`。
- **根因与常见错误处理**：本应为标量的单元格混入了 ndarray/list。Pandas 的分类、哈希和去重操作要求元素可哈希。
- **正确处理**：先定位对象列及异常值类型；根据业务含义把单元素数组解包为标量、多元素数组规范化为 tuple 或稳定字符串。不要粗暴对整张表 `astype(str)`，否则会掩盖数据结构错误并破坏数值与空值语义。
- **验证与防复发**：输出目标列的类型分布；确认规范化后不存在 ndarray/list，再运行原来的 categorical/sort/groupby，并核对行数与排序结果。

### P008｜跨月事实表重复累计主体静态属性

- **状态**：已确认
- **问题与适用范围**：价格变更分析把月度事实表与 Variant Base 属性直接汇总。
- **可观察表现**：运行 6 个月后，同一 Variant Base 的 Count、Cost、Price、调前价格和调后价格被累计 6 次。
- **根因与常见错误处理**：把"主体属性"与"期间指标"都按月度行求和，没有区分静态粒度和时间累计粒度。
- **正确处理**：Count、Cost、Price、调前/调后价格按 Variant Base 只取一次；Net Sales、Eligible 等期间指标按 Controller 完整日期范围汇总；状态以 Controller 结束日为准。
- **验证与防复发**：抽取一个跨多月存在的 Variant Base，确认静态字段只计一次，期间销售指标等于各月之和；整体 `nunique(variant_base)` 与 Count 口径一致。

### P009｜多次调价记录造成 Join 膨胀（已并入 P010）

- **状态**：已替代（并入 P010）
- **问题与适用范围**：本问题是右表键不唯一导致 N:N Join 的特定业务案例。
- **可观察表现**：同一主体因多次调价记录在合并后出现多行，销售、广告或金额字段随之重复。
- **根因与常见错误处理**：与 P010 相同：参与 Join 前，右表没有先压缩到目标粒度。

  常见错误处理：把项目专属的首条、末条或最新调价选择规则单独当作通用数据合并 Pitfall。
- **正确处理**：统一按 P010 处理。具体采用首条、末条或最新记录的业务规则留在来源项目。
- **验证与防复发**：参照 P010 检查 Join 前后的键唯一性、行数和控制总额。

  防复发：P009 编号永久保留，不再复用；新内容更新 P010。

### P010｜合并表后行数突然膨胀

- **状态**：已确认
- **问题与适用范围**：将 Shopify、广告、价格日志或分类映射等多张表合并。 价格日志中同一主体存在多条记录，是这一问题的常见实例；具体记录选择规则应留在来源项目。
- **可观察表现**：结果行数成倍增加，汇总金额异常变大，但代码没有报错。
- **根因与常见错误处理**：预期 1:1 或 N:1 的键在右表并不唯一，形成 N:N join。
- **正确处理**：合并前分别检查键的重复数；使用 Pandas `merge(..., validate='one_to_one'/'many_to_one')` 明确关系。需要聚合或选最新记录时，在 merge 之前完成。
- **验证与防复发**：记录 merge 前后行数与唯一键数；金额类字段做控制总额对账。

### P011｜用字符串化解决所有数据类型问题

- **状态**：已确认
- **问题与适用范围**：遇到 JSON、数组、日期、Decimal 或空值导致写入/排序失败时，直接对整表 `astype(str)`。
- **可观察表现**：代码暂时不报错，但出现字符串 `nan`、日期无法排序、数字在 Sheet 中成为文本、数组比较失真。
- **根因与常见错误处理**：用展示层转换掩盖了数据模型问题。
- **正确处理**：按列和业务语义规范化；数字保持数值、日期保持 datetime 或 ISO 字符串、空值统一为写入端可接受的空值、嵌套对象按明确规则展开或序列化。
- **验证与防复发**：检查关键列 dtype、Sheet 中数字格式、空值数量和日期排序；禁止仅以"能写进去"作为验收。

  ## 4. Google Ads 数据与校验

### P012｜把近期重新归因当成历史数据损坏

- **状态**：已确认
- **问题与适用范围**：本地 Google Ads 基座与 API 对账，尤其包含最近数天或近 30 天数据。
- **可观察表现**：Cost、Conversions、Conversion Value 等出现小幅差异；严格的 `差异 > 0 即 FAIL` 导致频繁失败。
- **根因与常见错误处理**：忽略 Google Ads 近期转化回补和重新归因，同时也不能因此对所有历史差异无限放宽。
- **正确处理**：保留 `API_RECONCILIATION_STRICT_MODE`。严格模式下任意差异按规则失败；非严格模式只允许近期、可解释且在明确阈值内的差异降级为 WARNING，历史严重偏差仍必须 FAIL。
- **验证与防复发**：报告中分开列出近期容差、历史偏差和实际阈值；用一条超阈值历史差异确认非严格模式仍会失败。

### P013｜混淆实体存在与期间活动状态

- **状态**：已确认
- **问题与适用范围**：构建广告数据基座或期间报表时，用当前启用状态或近期活动判断 Campaign、Ad Group 等实体是否存在。
- **可观察表现**：已暂停实体的历史事实消失，或真实实体因期间无活动被替换成 `No Data` 等占位身份。
- **根因与常见错误处理**：把 `entity existence` 与 `period activity` 合并判断，并把当前状态过滤错误地应用到历史事实或展示身份。
- **正确处理**：实体身份来自计划、配置或完整实体集合；Data Foundation 默认同步 ALL 状态。期间无活动只影响期间数值，状态只作为维度或筛选项，不得删除历史事实或改写实体身份。
- **验证与防复发**：抽查一个历史有数据但当前暂停的实体，以及一个期间无活动但仍在计划集合中的实体；前者历史事实仍存在，后者保留真实身份且期间值按规则为空或为零。

### P014｜把 ITEM_DAILY 当成 Asset Group 粒度

- **状态**：已确认
- **问题与适用范围**：Google Ads Shopping/PMax 商品日报按 `product_item_id` 获取数据。
- **可观察表现**：期望看到 Asset Group，但对应字段为空，容易误判为同步失败。
- **根因与常见错误处理**：输出粒度与期望维度不一致；`product_item_id` 粒度结果不保证提供可用的 Asset Group 归属。
- **正确处理**：在字段文档中明确 ITEM_DAILY 的实际粒度和限制。需要 Asset Group 分析时使用能稳定提供该维度的独立查询/数据集，不用空值强行补造。
- **验证与防复发**：检查查询资源与 segments；在输出说明中明确 Asset Group 是否可用，空值不计为同步错误。

### P015｜API 对账只看总额，不看日期与实体

- **状态**：已确认
- **问题与适用范围**：本地数据与 Google Ads API 总 Cost 恰好相同，但部分日期或 Campaign 错位。
- **可观察表现**：总额校验通过，Overview 仍出现缺行、重复或归类错误。
- **根因与常见错误处理**：总额相等不能证明数据完整；错误可能互相抵消。
- **正确处理**：至少分层校验总额、日期、账户、Campaign 和数据集主键；对关键指标同时比较行数、唯一键和汇总值。
- **验证与防复发**：故意交换两天的数据，确认总额测试虽然通过，但日期粒度校验会失败。

  ## 5. Shopify、商品与 Meta API

### P016｜`metafieldsSet` 返回 Access denied

- **状态**：已确认
- **问题与适用范围**：通过 Shopify GraphQL 批量写入 Product 或 Variant Metafield。
- **可观察表现**：GraphQL 返回 `Access denied`；其他商品读取或更新操作可能仍然正常。
- **根因与常见错误处理**：把"应用能访问商品"理解为"应用拥有全部 Metafield 写权限"，或没有检查目标 owner type 与 token scope。
- **正确处理**：检查 Admin API access scopes、应用安装后的实际授权、目标 owner ID/type 和 metafield definition 权限。权限未补齐前，明确标记该功能不可用，不能吞掉错误假装写入成功。
- **验证与防复发**：先对一个测试对象写入并立即 query 回读；确认 namespace、key、type、value 和 owner 均正确后再批量运行。

### P017｜ShopifyQL `parseErrors`

- **状态**：已确认
- **问题与适用范围**：通过 ShopifyQL 获取销售或商品分析数据。
- **可观察表现**：响应包含 `parseErrors`，可能没有标准 HTTP 失败码。
- **根因与常见错误处理**：只检查 HTTP 200，没有检查响应体中的语法错误；或沿用已变更/不支持的字段、日期和查询语法。
- **正确处理**：对响应同时检查 transport error、GraphQL/ShopifyQL error 和业务数据是否为空。记录查询阶段与安全的查询片段，按当前 ShopifyQL 语法逐段缩小定位。
- **验证与防复发**：先运行最小查询，再逐段加入字段和过滤条件；最终确认 `parseErrors` 为空且返回行数合理。

### P018｜Meta API 400 被当成可以自动重试的网络错误

- **状态**：已确认
- **问题与适用范围**：Meta API 返回 HTTP 400，曾见 `code=2, subcode=1504044`。
- **可观察表现**：无脑重试仍持续失败，或吞掉错误后留下部分结果。
- **根因与常见错误处理**：HTTP 400 往往是请求、对象状态、权限或平台侧业务限制，不等同于瞬时网络超时；仅凭 code/subcode 不能在没有响应上下文时下唯一结论。
- **正确处理**：保留 request stage、object ID、code、subcode、`error_user_title`/`error_user_msg` 等非敏感上下文；先判断是否可重试。只有明确的瞬时错误才指数退避，参数或权限错误应立即停止并修正。
- **验证与防复发**：修正后对单个对象重放请求并成功，再恢复批量；批量摘要必须报告成功、失败和跳过数量。

### P019｜多来源图片合并时顺序丢失或重复

- **状态**：已确认
- **问题与适用范围**：从 Each Box、SPU 或源商品合并产品图与 Variant 图。
- **可观察表现**：主图顺序变化、同一 URL 重复、较长的有效图片列表被较短列表覆盖。
- **根因与常见错误处理**：使用无序 set 去重，或简单以后一份列表覆盖前一份。
- **正确处理**：按已确认来源优先级顺序合并并稳定去重；保留最长有效列表；分别处理 `product.images_urls` 的 JSON 顺序与 `variant.image_url`。
- **验证与防复发**：用包含重复 URL、不同长度和 Variant 图的样本，核对最终顺序、数量和主图。

### P020｜批量 SEO 文案脱离商品标题事实

- **状态**：已确认
- **问题与适用范围**：批量生成 Product Title、SEO 或 Feed 文案。
- **可观察表现**：出现"选择尺寸后加入购物车"等非产品事实提示，或生成标题中不存在的规格和功能。
- **根因与常见错误处理**：模型用通用电商话术补充事实，而没有把产品标题和已确认字段作为事实边界。
- **正确处理**：产品标题与结构化字段是最终事实；禁止补造规格、兼容性、服务或购买流程提示。无法确认时保守省略。
- **验证与防复发**：抽查生成字段中的尺寸、材质、兼容性和功能，确保均能回溯到输入字段。

  ## 6. Notebook、文件和交付方式

### P037｜把单次远程大文件读取异常判定为正式数据损坏

- **状态**：已确认
- **问题与适用范围**：验证任务从远程 Drive 下载并检查大量 Parquet 文件。
- **可观察表现**：一次运行出现 Raw / Curated 异常或文件读取不完整，立即推断正式数据已经损坏。
- **根因与常见错误处理**：只依据单次传输或单次读取结果作结论，没有区分：
  - 远程传输的暂时性中断；
  - 不完整下载或缓存状态；
  - 可重复的数据一致性问题。
- **正确处理**：- 使用分块下载；
  - 对暂时性网络错误执行有限重试；
  - 校验文件大小和 Parquet 可打开性；
  - 重新运行同一范围；
  - 必要时使用另一 Runtime 复核关键计数；
  - 权限、Schema、配置和确定性数据错误仍应快速失败，不能无限重试。
- **验证与防复发**：只有在可重复读取后，才能把一致性差异归因于正式数据。若重试后 Local 与 Colab 的文件数、行数、Primary Key 和 Raw / Curated reconciliation 一致，则不得继续宣称数据损坏。

### P038｜多 Dataset 验证时 last_run_id 未对齐

- **状态**：已确认
- **问题与适用范围**：验证作业计划同时验证多个 Dataset，但此前只单独重跑其中一个 Dataset。
- **可观察表现**：各 Current Dataset 的 `last_run_id` 不一致，验证无法选择唯一的正式同步运行。
- **根因与常见错误处理**：跨 Dataset 验证需要一个可共同追溯的父运行；单 Dataset 重跑会改变局部状态，但不能自动代表其他 Dataset 属于同一次同步。
- **正确处理**：- 需要跨 Dataset 冻结结论时，在同一个父运行中同步全部计划 Dataset；
  - 只验证单个 Dataset 时，显式缩小验证范围；
  - 不得随意选择任意 `last_run_id` 来掩盖状态不一致；
  - 验证计划应在读取文件前检查 Dataset 状态是否对齐并快速失败。
- **验证与防复发**：- 所有计划 Dataset 指向同一个可核对的父运行；或
  - 验证范围明确限制为状态对齐的 Dataset；
  - Parent / Child Logs、写入分区和 Dataset Watermark 可以共同回溯。

### P050｜配置参数未贯穿真实执行链

- **状态**：已确认
- **问题与适用范围**：任务提供日期、分区、对象、恢复、清理、发布或其他配置，但该配置只存在于 Controller、界面、计划变量或部分检查，没有贯穿后续调用和真实副作用；适用于数据验证、同步、构建、写入和批处理任务。

  适用：带有人工参数、开关或运行边界的 Notebook、脚本、数据管道和批处理任务。
- **可观察表现**：界面或配置显示已缩小范围或启用某项能力，实际运行仍扫描范围外数据、跳过声称支持的路径，或没有发生预期副作用；耗时和处理量也可能与配置不符。
- **根因与常见错误处理**：配置只传递到 expected scope、Cell 1、注释或外层变量，没有作为同一约束继续贯穿执行调用、函数签名、索引筛选、文件读取、分支逻辑和外部副作用。

  常见错误处理：根据变量名、注释或 UI 判断功能已经完成；只修改计划摘要而不改变实际执行；或为加速而关闭核心验证。
- **正确处理**：让同一配置贯穿“Controller/界面 → 调用参数 → 函数签名 → 读取与分支逻辑 → 外部副作用 → 结果摘要”。范围外数据可以作为告警或审计证据，但不进入正式处理集合，除非任务明确要求全量审计。
- **验证与防复发**：针对每个对外参数追踪完整调用链，并用能区分开关前后结果的样例验证实际读取、处理统计和外部副作用。缩小范围后，读取量与处理量应相应下降；启用功能后，应出现对应且可核对的执行证据。

  防复发：任何对外配置都必须端到端传递到真实执行和结果证据，不能只停留在计划、展示、注释或预检查层。

### P055｜输入 Schema 变更未贯穿读取、校验和交付

- **状态**：已确认
- **问题与适用范围**：删除、增加或重命名 CSV、Excel、在线表格、YAML、JSON、Controller 或 Notebook 输入字段，或改变结构化输出和 View 的字段、过滤或追加契约时，只修改了输入文件、表面配置或中间层。

  适用：表格、结构化配置、Controller、Notebook、脚本和数据导入流程。
- **可观察表现**：正式模板已经删除无用字段，运行时仍报告缺少旧字段；新旧字段在读取、任务选择、路径解析和输出中混用；或过滤能力只在抽取或中间表生效，最终 View 没有体现。APPEND 时也可能把不同表头或动态列结构的数据写进同一张表。
- **根因与常见错误处理**：输入 Schema、字段常量、读取 Adapter、配置字典、校验器、默认值、中间表、View 构建、输出和操作说明被当成彼此独立的修改，没有作为一个端到端契约同步更新；测试又可能预先填好机器键或其他派生字段，只覆盖理想样例，掩盖真实输入依赖配置回退解析的路径。

  常见错误处理：让用户手工删列后继续使用旧代码；只修当前报错或中间层而不检查最终输出；在加载解析所需配置前便强制要求本可回退生成的机器字段；测试只覆盖机器字段已填写的样例；或仅比较首行/部分列便直接 APPEND。
- **正确处理**：一次完成“模板 → 常量 → 读取 → 配置加载与解析 → 校验 → 默认值 → 处理 → 中间表 → View/最终输出 → 文档”的全链路修改。必要时可在读取边界兼容旧 Schema，但正式模板和正式输出只保留已确认的最小字段。人类显示名称需要映射为机器键时，显式机器键作为 override；缺失时在配置加载后按显示名称及其作用域唯一解析，无匹配或多匹配才明确报错，成功后可按合同回写。两行表头、动态列表列或结构化 View 在 APPEND 前必须比较完整 Schema；不一致时拒绝追加、明确迁移或写入新版本输出。
- **验证与防复发**：用只包含最小正式字段、符合用户真实填写方式的全新输入执行 Dry Run，确认字段检查、任务选择、路径解析、配置回退、处理、中间表和最终输出均通过；映射测试至少覆盖显式 override、唯一回退、无匹配、多匹配和不同作用域同名；用改变过滤或输出结构的样例确认结果贯穿全链；再用旧格式样本验证已声明的兼容边界，并对 APPEND 执行完整表头契约测试。

  防复发：任何输入字段、过滤规则或输出 Schema 变化都必须执行完整链路检查，不能把模板或中间层修改视为独立交付。

### P057｜HTTP 失败状态未进入限速与重试策略

- **状态**：已确认
- **问题与适用范围**：浏览器自动化、API 抓取或受限站点流程中，低频顺序请求成功，但高频或并发流程返回失败 HTTP 状态；原重试器只在抛出异常时触发。

  适用：Playwright、浏览器自动化、API 抓取、代理流程、限速接口和失败补抓。
- **可观察表现**：一个流程正常，另一个并发流程大量返回 403 等失败状态；降低并发后成功率提高；失败响应被当作普通返回直接结束，没有退避重试或失败记录。
- **根因与常见错误处理**：不同流程的请求频率、并发、页面或会话创建方式不同；重试条件只覆盖 Exception，没有覆盖可识别的 HTTP 状态、空内容和解析失败。

  常见错误处理：仅凭一个流程失败就断言整个网络出口被封；只增加异常重试次数；维持高并发持续请求；丢弃最终失败对象。
- **正确处理**：先用单线程、单会话和小样本区分全面封禁与限速；对明确可重试的状态码、空内容和解析失败采用有限退避；必要时重建浏览器 Context 或会话；持续失败对象进入可恢复的失败队列。
- **验证与防复发**：比较高并发与低频小样本的成功率；确认日志包含重试原因、次数、退避、最终状态和失败记录，并验证失败对象可以在补抓模式中重放。

  防复发：抓取重试策略必须同时覆盖异常、HTTP 状态、空内容和解析失败，并按错误类别决定快速失败、退避或补抓。

### P058｜历史字段名称代替业务语义且缺少关系校验

- **状态**：已确认
- **问题与适用范围**：迁移历史数据或重构 Schema 时，旧字段名称与真实业务含义不一致，页面或来源中又存在多个相近数值语境。

  适用：历史数据迁移、Schema 重构、解析器兼容、财务和价格字段、单位换算及旧系统升级。
- **可观察表现**：仅交换表头后数值仍落入错误字段；多个语义不同的字段得到相同值；旧数据直接追加到新 Schema 后看似结构正确但业务含义错误。
- **根因与常见错误处理**：迁移前没有冻结字段的业务定义，把 Parser 输出名或历史表头当作真实含义的唯一证据，也没有为关键数值建立单位、数量或总分关系校验。

  常见错误处理：只交换列名；直接复用旧 Parser 输出；把旧数据无校验地追加到新 Schema；修改已验证的核心解析逻辑却不保留兼容边界。
- **正确处理**：先用来源证据冻结每个字段的业务语义，再在正式输出 Adapter 中完成清晰映射；保留已验证的核心提取逻辑，把兼容和语义重命名放在边界层；对存在数量、单位、单价和总价关系的字段建立一致性校验，旧输出由代码自动升级。
- **验证与防复发**：使用覆盖不同业务语境、含数量关系和不含数量关系的真实样本，逐项核对字段含义，并验证适用的计算关系；不适用字段保持为空而不是复制相近值。

  防复发：任何历史字段迁移前必须先冻结业务语义；字段名称不能作为含义的唯一证据；关键数值字段必须有可执行的关系校验。


## 4. Notebook、Runtime 与文件交付

### P021｜所有流程塞进一个超大 Colab

- **状态**：已确认
- **问题与适用范围**：把创建、同步、清洗、校验、构建 Overview 和写入全部放入一个 Notebook。
- **可观察表现**：任一阶段出错都难以定位，修改局部逻辑需要理解整本 Notebook，重新运行成本高。
- **根因与常见错误处理**：以"文件少"为目标，牺牲了职责边界和可维护性。
- **正确处理**：按清晰 Job 拆为多个职责单一的 Notebook，例如 Create → Sync → Validate → Build；共享规则通过 Console、配置或明确公共模块复用。
- **验证与防复发**：每本 Notebook 都有独立 Controller、输入输出、运行摘要和验收条件；可以单独重跑而不依赖手工隐藏状态。

### P022｜每次修改都生成"最新版_v2_final_final"

- **状态**：已确认
- **问题与适用范围**：仓库或正式资产目录中，同一 Notebook、脚本或文档被反复创建模糊命名的平行副本。本条管理仓库 Current；聊天下载交付的防缓存命名见 P060。
- **可观察表现**：目录中存在多个看似最新的文件，无法判断正式入口，链接和文档持续失效；重命名后还可能出现 `.ipynb.ipynb`；临时输出、旧脚本和中间方案因为被当作"过程证据"长期占据 Current。
- **根因与常见错误处理**：没有区分版本化候选、稳定 Current、历史 Archive 和临时过程产物，或重命名前没有核对实际扩展名。
- **正确处理**：候选可以携带版本号和修复说明；正式 Current 使用稳定、无版本号的操作型名称和 URL；历史版本进入 Archive；有效知识进入权威文档，不能让所有临时输出、旧脚本和中间方案长期占据 Current。移动或重命名前核对实际文件名和目标扩展名；Notebook 内部版本保留在元数据或代码中；文件是否删除由用户逐项确认。
- **验证与防复发**：目录中能一眼识别唯一 Current；同一功能不留下多个看起来像当前版本的入口；历史版本在 Archive 中按名称可排序；交接文档只指向正式入口；文件只有一个正确扩展名，内部版本可从 Notebook 自身读取。

### P023｜直接交付 XLSX，而可维护的 Colab 更合适

- **状态**：已确认
- **问题与适用范围**：需要一次生成很多结构类似的表格文件，或未来还会反复调整字段和格式。
- **可观察表现**：逐个生成文件耗时，后续规则变化需要全部重做，用户无法自行复跑。
- **根因与常见错误处理**：没有在产出前比较交付格式的生成速度、复用性和维护成本。
- **正确处理**：产出前先判断并推荐最适合的形式。一次性定稿数据可直接 XLSX；重复生成、批量结构和未来可调参数通常优先完整 Colab；长文档优先 Markdown，只有版式要求高时使用 DOCX/PDF。
- **验证与防复发**：交付方式能覆盖当前需求，并说明后续修改入口；用户不需要靠重新发起同样任务才能复现。

### P024｜只给补丁，没有给完整 Notebook

- **状态**：已确认
- **问题与适用范围**：用户要求"新的完整 Colab"，交付却只有需要手工复制的 Cell 或代码片段。
- **可观察表现**：用户无法确定片段插入位置，旧代码残留，新旧版本混杂。
- **根因与常见错误处理**：把开发差异当成最终用户产物。
- **正确处理**：当用户明确要求完整 Notebook 时，交付可从头运行的完整 `.ipynb`；补丁只用于解释，不替代最终文件。
- **验证与防复发**：在干净环境打开 Notebook，自 Cell 1 起可顺序运行；文件名、版本记录和 Controller 完整。

  ## 7. Sheets 展示与 Operator-First 设计

### P034｜双 Runtime 不应维护两套业务代码

- **状态**：已确认
- **问题与适用范围**：同一个自动化任务需要：
  - Colab 运行；
  - VSCode 本地运行。
- **可观察表现**：维护两份逻辑相同但环境不同的代码。
- **根因与常见错误处理**：把运行环境差异混入业务逻辑。
- **正确处理**：保持：

  Business Logic

  单一。

  通过：
  - Secret Loader
  - Runtime Adapter
  - Environment Config

  处理环境差异。
- **验证与防复发**：同一个 Notebook：
  - Colab 可运行；
  - Local Kernel 可运行；
  - 输出结果一致。

### P035｜混淆 Local 依赖安装与 VSCode 环境注入生命周期

- **状态**：已确认
- **问题与适用范围**：同一个 Notebook 需要在 Local VSCode 中使用专用虚拟环境和本地 Secret。
- **可观察表现**：- Local Kernel 缺少运行依赖；
  - 从桌面或 Finder 启动 VSCode 后读不到终端中的环境变量；
  - VSCode 界面中选了看似正确的 Python Interpreter，但 Notebook 实际使用的 Jupyter Kernel、metadata 或进程环境并不一致；
  - 误以为每次运行前都需要重新安装依赖。
- **根因与常见错误处理**：把三个不同生命周期混为一体：
  - 虚拟环境创建；
  - 依赖安装；
  - 每个 VSCode 进程的环境变量注入。

  同时混淆四个不同层次：
  - Python Interpreter；
  - Jupyter Kernel / kernelspec；
  - Notebook metadata 中记录的 Kernel 选择；
  - VSCode 或 Jupyter 进程实际继承的环境变量。
- **正确处理**：- 为项目建立专用虚拟环境；
  - 每个项目只保留一套明确的虚拟环境，通过依赖清单和安装脚本重建，不压缩、分发或提交整个虚拟环境目录；
  - 确认真正被选中的 Jupyter kernelspec，并让它指向同一个虚拟环境；
  - 依赖通常每个虚拟环境安装一次；
  - Local Notebook 缺依赖时快速失败并打印精确安装命令；
  - 共享包已在其他虚拟环境安装但当前 Notebook 报 `ModuleNotFoundError` 时，以当前 Kernel 的 `sys.executable` 为准，使用该解释器执行 `-m pip install -e <shared-package>`，不要用共享包自身环境的安装结果推断 Notebook 已可导入；
  - 需要本地环境变量时，通过本地启动器启动新的 VSCode 进程；
  - 排查时分别核对 Interpreter 路径、kernelspec resource directory、Notebook metadata 和进程环境，不用 UI 中的 Interpreter 选择替代 Kernel 验证；
  - 明确区分虚拟环境、Secret 键值配置和 Kernel 注册引用；Secret 使用项目专用映射文件，程序只读取唯一指定映射，不扫描或混用其他项目配置；
  - 启动器和 Secret 文件保留在不参与云同步的本地目录，并由 Git 忽略。
- **验证与防复发**：- 当前 Kernel 的 Python 路径属于目标虚拟环境；
  - kernelspec、Notebook metadata 和当前 Kernel 的 Python executable 指向同一套预期环境；
  - 依赖预检和共享包 import 通过；安装或更新共享包后重启 Kernel，再从干净 Kernel 验证；
  - 新 VSCode 进程能读取所需 Secret 名称；
  - Secret Loader 只读取当前项目指定映射，日志不包含 Secret 值；
  - Git 不包含虚拟环境目录、Secret 文件或凭据文件；
  - Notebook 不在 Local Runtime 中自动安装或修改依赖。

### P036｜静态检查通过后未做 clean-kernel Run All

- **状态**：已确认
- **问题与适用范围**：Notebook 已通过语法或静态编译检查，但正式运行时出现未定义名称或遗漏导入。
- **可观察表现**：运行到实际初始化路径时出现 `NameError`，例如使用 `defaultdict` 但没有对应导入。
- **根因与常见错误处理**：静态检查只能证明部分语法成立，不能证明：
  - 所有运行路径所需名称已导入；
  - Cell 顺序正确；
  - 当前 Kernel 没有依赖历史状态；
  - 从零开始可以完整执行。
- **正确处理**：正式候选执行：

  Restart Kernel ↓ Run All

  不要用旧 Kernel 中残留的变量掩盖缺失导入或 Cell 顺序问题。
- **验证与防复发**：- 干净 Kernel 从 Cell 1 开始执行；
  - 所有 Cell 按顺序完成；
  - 没有依赖手工预先运行的隐藏状态；
  - 再依据任务风险完成 Dry Run 或 live acceptance。

### P048｜Notebook 输出和 metadata 改变文件身份并污染 Git diff

- **状态**：已确认
- **问题与适用范围**：`.ipynb` 的输出、execution count、metadata、Kernel 信息和保存动作都会改变文件字节，并可能把未计划修改的 Notebook 标记为 modified；适用于 Jupyter Notebook、Colab、可执行文档、Git 提交和需要 exact-current evidence 的数据流程。
- **可观察表现**：业务代码没有变化，但运行、清除输出、切换 Kernel 或保存 Notebook 后，完整文件 Hash 与提交时不同；Git diff 主要出现运行输出、执行计数或环境 metadata，随后难以判断运行证据是否对应当前字节，也容易把噪音与正式源码改动一起提交。
- **根因与常见错误处理**：把 Notebook 当成纯源码文件，忽略输出、执行计数和 metadata 也是 `.ipynb` JSON 的一部分。

  常见错误处理：把 Hash 不一致直接判定为代码损坏，反复重跑或修改源码；反过来用运行后的 Hash 证明提交前的 Current；或未检查 diff 就把输出和 metadata 噪音与正式代码一起提交。
- **正确处理**：在使用完整字节身份绑定前，先规定 Notebook 输出和 metadata 的处理方式。Current 编辑源应与运行输出分离；必要时使用执行副本、清除临时输出后再提交，或在 manifest 中明确记录运行对应的实际 Notebook 字节身份。提交前逐个核对 modified Notebook，只提交授权的源码变化；查看或运行后不需要保留输出时，关闭前不要保存。历史证据只能证明它绑定的字节，不能自动升级为当前文件证据。文档引用 Notebook 字节身份时，要同步处理文档和 Notebook 的提交边界；不能让文档声称当前字节已验证，却不提交对应的已授权源码变化。
- **验证与防复发**：比较运行前后完整文件 Hash，并核对代码单元内容、Notebook metadata、Kernel 信息和证据 manifest 中记录的身份是否一致；提交前检查 Git 状态、diff stat 和目标文件 diff，确认变更文件及内容与授权范围一致；核对文档引用、staged 文件、Notebook dirty 状态和提交范围是否一致。

  防复发：Notebook 项目提交前必须确认每个 modified 文件是否包含真实源码变化；凡是声称 Notebook exact-current evidence 的流程，都必须先定义输出、execution count、metadata 和保存动作的处理规则，否则只能标记为较弱证据或 `EVIDENCE_GAP`。Evidence 成熟度、Current 身份和提交边界必须分开记录并一起核对。

### P052｜macOS launchd 后台任务直接访问受保护目录失败

- **状态**：已确认
- **问题与适用范围**：macOS 上使用 `launchd` 定时触发 Notebook、Python、Jupyter nbconvert、本地 ETL、数据同步或其他自动化脚本时，后台进程直接读取用户保护目录下的 cwd、runner、Notebook、输入、输出或日志路径。

  适用：macOS `launchd`、Notebook runner、Jupyter nbconvert、Python 脚本、本地 ETL、数据同步和自动化调度任务。
- **可观察表现**：手动终端运行成功，但 `launchd` 触发后出现 `PermissionError: Operation not permitted`；脚本或 nbconvert 可以启动，却在 `os.getcwd()`、打开 Notebook 文件、读取输入或写日志时失败。
- **根因与常见错误处理**：macOS 对 Documents、Desktop、Downloads 等用户保护目录有额外隐私权限限制；后台调度任务即使通过 shell、Python 或 nbconvert 间接执行，也可能在实际打开受保护目录中的文件时被拦截。只把 cwd 改到非保护目录不足以解决问题，因为被执行器实际读取或写入的文件本体仍可能位于受保护目录。

  常见错误处理：反复给无关应用开启 Full Disk Access；把问题误判为 Secret、Kernel、Notebook 逻辑或路径拼写错误；只执行 `cd /tmp` 或更换 cwd 就认为权限绕行完成；让 `launchd` 继续直接读取受保护目录中的源码 Notebook 或 runner。
- **正确处理**：将定时执行所需的 runner、Notebook 执行副本、输入输出、logs 和 runs 放到非保护目录的本地 Runtime 工作区；`launchd` 指向 Runtime runner；源码 Current 仍由项目仓库维护。排查时同时检查 cwd、script path、notebook path、input path、output path 和 log path，确认执行链中被后台任务实际打开的文件本体都避开受保护目录。
- **验证与防复发**：手动运行 Runtime runner 通过；`launchd` manual trigger 通过；stderr 没有真实错误；日志出现完整完成标志；nbconvert 命令的实际输入 Notebook 路径来自 Runtime 位置。

  防复发：macOS 后台调度任务不要把受保护目录作为 cwd，也不要直接执行或读写受保护目录中的依赖文件；优先采用非保护目录 Runtime 执行副本，并记录 Current 与 Runtime copy 的刷新规则和证据边界。

### P053｜Jupyter/nbconvert TCP warning 不等于任务失败

- **状态**：已确认
- **问题与适用范围**：本地 Jupyter、ipykernel 或 nbconvert 执行 Notebook 时出现内核通信 warning；适用于本地 Notebook 自动化、定时执行和命令行执行。

  适用：本地 Jupyter、ipykernel、nbconvert、Notebook runner 和定时 Notebook 自动化。
- **可观察表现**：执行日志中出现 `[IPKernelApp] WARNING | Kernel is running over TCP without encryption...`，但 Notebook 仍可能继续执行并产出结果。
- **根因与常见错误处理**：这是本地 Jupyter kernel 通信方式相关 warning，不等于 Notebook 业务逻辑、Kernel 启动或 nbconvert 执行失败。

  常见错误处理：看到该 warning 就中断任务或误判失败；反过来只因为 warning 可忽略，就跳过退出状态和输出检查。
- **正确处理**：区分 warning 与 failure；以 Traceback、进程退出状态、executed notebook 是否写出、业务输出是否生成、stderr 是否有真实错误、任务完成标志是否出现作为判断依据。
- **验证与防复发**：等待执行结束，检查退出状态、Traceback、输出 notebook、业务输出、stderr 和完成标志；只有满足任务验收条件才判定成功。

  防复发：Notebook 自动化验收不得只看 warning 文本；必须用明确的失败信号和完成证据判断任务状态。

### P054｜同名远程 Python 模块导致旧代码继续运行

- **状态**：已确认
- **问题与适用范围**：Python 模块通过 GitHub raw、Notebook/Colab 下载、远程路径、本地重复下载或 `importlib` 动态加载后，修复版本继续沿用旧文件名；适用于远程模块、Notebook 外部 `.py`、脚本热更新和多 Runtime 工作流。

  适用：GitHub raw 模块、Colab Notebook、本地 Jupyter、`importlib` 动态加载、脚本热更新、双 Runtime 或其他存在缓存和旧模块复用风险的 Python 工作流。
- **可观察表现**：修复文件已经生成或覆盖，但重新运行后仍出现与旧代码一致的报错；运行输出无法证明当前加载的是新文件还是缓存、旧下载文件或 Runtime 中已有模块对象。
- **根因与常见错误处理**：同名交付文件在下载缓存、Notebook 已有本地副本、Python import 缓存、`importlib` 缓存或 `sys.modules` 既有模块对象等层面都可能被复用；缺少模块内版本和实际加载路径输出时，无法区分“修复无效”和“修复根本未被加载”。

  常见错误处理：继续用相同文件名覆盖远程或本地文件，只要求用户重跑调用 Cell；失败后才建议重启 Runtime，但没有版本证据。
- **正确处理**：每次交付这类远程加载的 Python 模块时，默认使用全新版本化文件名；代码内声明可读的模块版本；模块加载或主运行入口打印 repo、branch、commit、实际 `__file__`、模块文件名和版本号；Notebook 下载地址、保存路径和加载路径同步指向同一来源。必要时再清理 `sys.modules`、刷新 import 缓存或重启 Runtime。
- **验证与防复发**：运行模块加载 Cell 后，控制台必须打印预期的 repo、branch、commit、文件名和版本号；检查 `module.__file__` 指向预期文件；执行目标函数时仍输出同一版本，再验证原问题是否消失。

  防复发：凡是存在远程下载、Notebook 重复运行或动态 import 的 `.py` 修改，不得只覆盖旧文件名并假设新代码已生效；必须执行“新版本化文件名 + 内置版本 + 加载或运行时打印来源 revision、实际文件路径和版本”的组合。

### P060｜聊天交付同名文件导致缓存或旧附件混淆

- **状态**：已确认
- **问题与适用范围**：ChatGPT、浏览器下载或聊天附件中交付修改后的 Notebook、Markdown、XLSX、ZIP 或其他完整文件。
- **可观察表现**：用户下载后仍打开旧内容；同名文件被浏览器、附件列表或本地下载目录复用；无法确认当前拿到的是哪一次交付。
- **根因与常见错误处理**：聊天交付件沿用旧文件名，而下载缓存、重复文件自动命名和旧附件同时存在。仓库稳定 Current 命名规则被错误套用到临时下载交付场景。

  常见错误处理：继续覆盖同名下载文件，或只在正文中声明“这是新版”而不给可识别的新文件名。
- **正确处理**：除 1–2 个 Cell 的纯文本小修外，完整文件交付使用新的明确文件名，可包含日期、修复主题或交付批次。用户确认后，再将内容放回仓库的稳定 Current 名称；不要让下载文件名反向成为仓库 Current。
- **验证与防复发**：下载后检查文件名、内部版本或关键改动，确认与本次交付一致；同一聊天中的旧附件不会被误认为当前交付。

  防复发：明确区分“聊天下载交付名”和“仓库 Current 名”：前者防缓存使用新名称，后者保持稳定并原位更新。


## 5. 输出、展示与知识资产

### P025｜宽表视觉结构无法支持快速定位

- **状态**：已确认
- **问题与适用范围**：经营报表、Google Sheets Overview 或其他横向较宽、包含重复业务区块和同级实体的输出。
- **可观察表现**：使用三层以上表头时阅读路径过长；数据区完全无色时容易看错行；同级 Campaign 或重复区块完全同色时边界不清。
- **根因与常见错误处理**：把抽象层级或视觉简洁放在实际阅读效率之前，没有同时表达业务块、数据行和同级实体边界。

  常见错误处理：继续增加表头层级，或取消所有数据区定位辅助；也不能为每个实体随机使用高饱和颜色。
- **正确处理**：优先使用两层信息结构；通过业务块色系、非常浅的数据区底色、斑马线、边框、留白和同色系深浅交替表达层级。同级实体应可区分，但整体仍保持统一。
- **验证与防复发**：在正常缩放、冻结区域和横向滚动状态下随机定位数值，确认能快速回溯到正确行、指标和实体；灰阶或打印时仍有边框、留白或斑马线辅助。

  防复发：宽表设计验收必须同时检查表头层级、数据区行定位和同级实体边界，不只检查数值是否存在。

### P026｜数据区完全无色，容易看错行（已并入 P025）

- **状态**：已替代（并入 P025）
- **问题与适用范围**：本条是 P025“宽表视觉结构无法支持快速定位”的一个具体表现。
- **可观察表现**：横向查看多个指标时容易把 A6 看成 A7，尤其在长 Campaign 表中。
- **根因与常见错误处理**：与 P025 相同：视觉结构没有同时支持行定位、区块层级和同级实体分隔。

  常见错误处理：继续将这一表现作为独立规则维护，造成相近条目重复。
- **正确处理**：统一按 P025 处理和更新。
- **验证与防复发**：参照 P025 的综合视觉定位验收。

  防复发：P026 编号永久保留，不再复用；新内容更新 P025。

### P027｜不同 Campaign 使用完全相同的色块（已并入 P025）

- **状态**：已替代（并入 P025）
- **问题与适用范围**：本条是 P025“宽表视觉结构无法支持快速定位”的一个具体表现。
- **可观察表现**：Campaign 边界不明显，纵向扫描时容易串行。
- **根因与常见错误处理**：与 P025 相同：视觉结构没有同时支持行定位、区块层级和同级实体分隔。

  常见错误处理：继续将这一表现作为独立规则维护，造成相近条目重复。
- **正确处理**：统一按 P025 处理和更新。
- **验证与防复发**：参照 P025 的综合视觉定位验收。

  防复发：P027 编号永久保留，不再复用；新内容更新 P025。

### P030｜Markdown 长表格不适合作为工具资产索引

- **状态**：已确认
- **问题与适用范围**：整理 Photoshop JSX、脚本、工具等个人资产索引时，尝试使用 Markdown 表格记录文件名、用途、参数和说明。

  适用：- Script Index
  - Tool Registry
  - 自动化工具说明
  - 个人知识资产目录

  不适用：- 少量字段横向比较
  - 数据分析结果表
  - 需要批量筛选的数据清单
- **可观察表现**：长文件名、长参数列表导致表格换行混乱；在 Obsidian 中阅读困难，横向比较反而降低查找效率。
- **根因与常见错误处理**：把"结构化"简单等同于"表格"。Markdown 表格适合短字段比较，不适合承载长文本说明、代码参数和工具使用场景。
- **正确处理**：工具类资产优先使用信息卡结构：
  - 标题（文件名）
  - 用途
  - 输入
  - 输出
  - 什么时候用
  - 可配置参数
  - 主要修改位置
- **验证与防复发**：打开 Obsidian 检查长内容阅读体验；确认 AI 能通过固定结构快速理解工具职责。

### P031｜文件名应表达能力，不应误导默认参数

- **状态**：已确认
- **问题与适用范围**：整理 Photoshop JSX 工具名称时，发现部分文件名包含固定尺寸，例如 `140x140`，但代码内部尺寸参数实际可调整。
- **可观察表现**：文件名让使用者误认为工具只能生成某个固定尺寸。
- **根因与常见错误处理**：把当前默认配置值当成工具能力。
- **正确处理**：- 固定能力才写入文件名，例如 `JPG_Max2000px`。
  - 可调整参数使用 `SizePx`、`Configurable` 等表达。
- **验证与防复发**：仅看文件名即可判断工具用途，但不会误解其限制。

### P045｜重复区块的固定语义只写入首个区块

- **状态**：已确认
- **问题与适用范围**：在横向或纵向重复的报表区块中生成 Sum、Compare 等固定标签或其他固定语义。
- **可观察表现**：首个区块标签完整，后续区块缺失；数值可能存在，但读者无法确认行列语义。
- **根因与常见错误处理**：固定语义只按全表首列或首个区块写入，没有把它作为区块模板的一部分逐块生成。

  常见错误处理：只修首个可见缺口，或只校验数值而不校验固定标签。
- **正确处理**：为每个重复区块生成并写入固定标签，把标签位置和值纳入 reconciliation。
- **验证与防复发**：遍历首个、中间和最后一个区块，确认固定标签完整且与区块定义一致；故意删除一个标签时 reconciliation 必须失败。

  防复发：重复区块的固定语义必须按区块生成、写入和验证。

### P046｜分隔列只修复可见区域或单个 Tab

- **状态**：已确认
- **问题与适用范围**：Google Sheets 多 Tab 宽表使用分隔列区分重复业务区块。
- **可观察表现**：顶部区域已变为预期背景，中部仍继承 banding 或条件格式；不同 Tab 的分隔列宽度不一致。
- **根因与常见错误处理**：把分隔列当成局部单元格修饰，只格式化部分行或单个 Tab，没有建立跨 Tab 的整列组件规则。

  常见错误处理：逐个修补当前可见范围，未检查整列、条件格式覆盖和其他 Tab。
- **正确处理**：把分隔列作为跨 Tab、整列级视觉组件，集中生成背景、条件格式优先级和统一宽度规则。
- **验证与防复发**：在每个适用 Tab 检查顶部、中部和底部单元格，以及列宽和条件格式；所有分隔列应呈现一致。

  防复发：分隔列规则必须集中生成，并执行跨 Tab、整列范围验证。

### P056｜文件格式只通过程序读取，未在目标客户端验收

- **状态**：已确认
- **问题与适用范围**：为普通办公软件导出分隔文本或其他交换文件，只用程序库回读验证，没有使用用户实际客户端打开。

  适用：CSV、TSV、文本导出、运营报表、数据交换和办公软件兼容。
- **可观察表现**：程序读取出的列结构正确，但用户直接打开文件时所有字段黏在同一列，或出现编码、换行和数字格式问题。
- **根因与常见错误处理**：把解析库能够识别分隔符当成目标客户端也会自动识别，忽略不同软件、区域设置和打开方式的导入行为差异。

  常见错误处理：怀疑业务数据损坏；要求用户每次手工分列；继续交付只在开发环境中验证过的格式。
- **正确处理**：面向普通表格软件的正式文本输出优先选择目标客户端可直接识别的格式和编码；如必须保留特定分隔格式，应提供明确导入方式，不能假设双击即可正确分列。
- **验证与防复发**：用用户实际使用的软件直接打开正式输出，检查列结构、字符编码、换行、日期和数字；程序回读只能作为补充验证。

  防复发：文件格式验收必须包含目标客户端打开测试，不能只做解析库或 DataFrame 回读测试。


## 6. 文档、Git 与知识治理

### P039｜关键文档压缩造成语义丢失

- **状态**：已确认
- **问题与适用范围**：重写或大幅压缩 Handoff、Master Plan、Document Map、Handoff Index 等关键项目文档，或在跨阶段、跨窗口、Source Mode 切换时传递知识候选，仍有效的运行入口、资源边界、风险、故障经验、验收规则、停止条件或待处置知识可能被删除；适用于所有承担运行、排障、验收、交接或知识吸收职责的载体。
- **可观察表现**：Diff 显示大量删除或整体重写，新文档只保留最新状态；格式检查通过，但旧版中的有效事实在新版无对应位置；前一阶段已判定需要吸收的知识候选，在后一阶段因来源上下文不可读而被错误判为无需处理。
- **根因与常见错误处理**：只按重复率或篇幅压缩，只保留最新状态，没有比较新旧版本的语义，并默认旧内容都已过时；或把 Source Mode 当作候选事实的唯一来源，没有把尚未处置的知识摘要作为跨阶段状态持续传递。

  常见错误处理：因为新版更短就直接提交；只运行 Markdown 格式检查或只看 diff stat；看到大量删除却不逐项核对事实；切换窗口或 Source Mode 后丢弃此前已识别的候选，并以当前阶段无法重读原始上下文为由改判无需吸收。
- **正确处理**：提交前执行只读语义保全审查，将删除内容分类为 `SUPERSEDED_CORRECTLY`、`DUPLICATE_REMOVED`、`HISTORICAL_ONLY`、`MOVED_TO_CORRECT_DOCUMENT`、`VALID_CONTENT_LOST`、`NEEDS_CONFIRMATION` 或 `OUT_OF_SCOPE_CHANGE`。存在 `VALID_CONTENT_LOST` 或 `OUT_OF_SCOPE_CHANGE` 时不得提交，只定向恢复有效内容，不机械恢复整份旧文档。owner 职责迁移时，源文件中的相应删除和目标 owner 的完整承载必须同时进入获批 staged scope，不能只提交迁移的一侧。已识别但尚未处置的 PKB 候选必须以 `PKB_SOURCE_SUMMARY` 持续携带，直至明确进入 `ABSORBED_INTO_PKB`、`ALREADY_COVERED_IN_PKB`、`USER_DECLINED` 或 `INVALIDATED_WITH_REASON` 之一。
- **验证与防复发**：比较 HEAD 与工作树版本，逐项核对运行、配置、风险、故障、验收和停止条件，再运行 `git diff --check`；职责迁移还必须检查源文件删除、目标 owner 承载和 staged scope 同时完整，并明确确认 `VALID_CONTENT_LOST: NO`；跨阶段流程还应逐项核对 `PKB_SOURCE_SUMMARY` 中的候选及其最终处置状态，确保没有无理由消失或改判。

  防复发：关键文档出现高删除比例、整体重写、职责变化、大量迁移，运行、排障、验收、停止条件被压缩，或知识吸收流程切换阶段、窗口、Source Mode 时，语义保全审查是提交或结束流程的门禁。

### P040｜把已经完成的状态误判成前置条件失败

- **状态**：已确认
- **问题与适用范围**：自动化只接受“尚未执行”的理想前置状态，遇到目标操作已经完成时错误停止；适用于 Git、文件迁移、发布、资源创建及其他应具备幂等性的流程。
- **可观察表现**：流程要求位于功能分支，但实际已在主分支且主分支与目标分支指向同一提交；工具仍报告前置条件失败，并跳过可继续执行的只读验证。
- **根因与常见错误处理**：流程只定义成功和失败，没有定义“操作前已经处于目标状态”。

  常见错误处理：机械执行重复操作，或因前置状态不符合脚本预期就判失败并停止验证。
- **正确处理**：结果必须区分 `ACTION_COMPLETED_NOW`、`ALREADY_IN_DESIRED_STATE` 和 `BLOCKED`。合并场景中，若主分支与目标分支已指向同一提交，判定 `ALREADY_FAST_FORWARDED`，不重复合并但继续验证；若无法 fast-forward，则判定 `BLOCKED`，不自动创建 merge commit、rebase 或 squash。流程、Prompt 或 Skill 更新后，不自动重跑已经完成且已提交的历史工作；只有当重跑会改变真实结论、暴露遗漏或降低现存风险时才重新执行。
- **验证与防复发**：比较分支引用和 merge-base，检查 HEAD 与工作树，并判断目标结果是否已经实际达到。

  防复发：所有可重复运行的提示词和自动化都必须把“已达成”设计为独立成功状态；状态识别与结果验证不得绑定为同一个开关。工具改进只在下一次真实任务中自然验证，除非它确实改变决策、产出或风险。

### P041｜混淆 Current 身份与验收证据成熟度

- **状态**：已确认
- **问题与适用范围**：代码、Notebook、数据管道、正式文档和发布资产需要同时判断 Current 身份、证据成熟度、用户验收和 Git 状态时。
- **可观察表现**：文件被标记为 Current 就被宣称已经验收或冻结；新版因缺少同等级运行证据被撤销 Current；旧版证据被自动继承给新版；工作树干净被当作全部文档状态已经同步。
- **根因与常见错误处理**：把“当前权威入口”“验证证据”“用户确认”“Git 提交”和“文档状态”压缩成一个状态。

  常见错误处理：用其中任一状态替代其他状态，或把无法绑定当前版本的历史证据描述成当前版本的精确证据。
- **正确处理**：分别记录 Current 身份、Evidence Maturity、用户验收、Git 状态和文档状态。Current 可以存在 `EVIDENCE_GAP`；旧证据只能证明其绑定的版本。Runtime copy、Notebook 字节身份和 dirty worktree 分别按 P052/P054、P048 和 P047 处理。
- **验证与防复发**：逐项列出上述状态并核对来源，确认没有一项被另一项替代；发生合并或冻结后，定向检查适用权威文档中的分支、revision、状态和 Next Action。

  防复发：任何“完成、验收、冻结或 Current”结论都必须说明它具体指哪一层状态。

### P047｜把未跟踪文件直接当作垃圾删除

- **状态**：已确认
- **问题与适用范围**：项目收口或 Current 整理时发现未跟踪文件、已说明的本轮修改或来源不明的工作树修改，需要判断其是候选、重复资产、历史版本、收口来源、未知风险、运行日志、临时 runner、Notebook 输出噪音，还是可删除临时文件。
- **可观察表现**：仅凭 `git status` 中的 untracked 状态就计划删除，尚未确认它是否承载唯一内容、Current 身份或应归档历史；或把所有 dirty worktree 都视为同一种异常，导致已说明修改也被阻断；项目收口时授权文档、必要代码变更、Notebook metadata/output、运行日志和过时临时脚本混在一起。
- **根因与常见错误处理**：把版本控制状态误当成内容价值和资产身份，没有比较完整 Hash、内容语义、Current 依据和归档目标；没有区分已由当前任务说明的修改和来源不明的修改。

  常见错误处理：直接批量删除未跟踪文件；只比较文件名或短 Hash；看到近似副本就假定内容相同；为了取得 clean 状态擅自 stash、restore、reset 或覆盖；直接 `git add .`；把运行日志、临时 runner、Notebook 输出噪音和正式源码改动一起提交；把已说明修改当作未知风险停止。
- **正确处理**：删除前逐项比较完整 Hash、内容语义、Current 身份和预期归档目标，展示 diff、定向变更计划和精确删除范围；已说明修改可以作为收口来源继续只读审计；未知修改才触发停止或 Dirty Worktree Triage；无法确认身份时停止并请求用户判断。收口前先做 dirty worktree disposition audit，将文件分为 stage、keep unstaged、restore、delete、ignore 或 needs confirmation；特别区分 Notebook 源码变化、Notebook 输出/metadata、日志、临时 runner 和文档证据引用。与限定范围任务无关的历史 staged 修改不应自动阻塞任务，也不得被取消暂存、覆盖或改写；保持其原状，仅重新 stage 本次获批路径，并使用限定路径提交或等价隔离方式，避免把无关内容混入本次提交。
- **验证与防复发**：删除计划能够为每个目标给出完整 Hash、与保留资产的语义关系、Current/Archive 归属和明确去向；未确认项不执行删除；工作树审计能标出每项修改是已说明来源还是未知来源。提交前检查 `git status --short`、`git diff --cached --name-status` 和 `git diff --cached --check`，核对本次提交的 staged scope 与获批路径一致；如存在无关历史 staged 内容，还要确认其保持原状且限定路径提交或等价隔离不会将其纳入。

  防复发：未跟踪只表示未纳入 Git，不表示可删除；任何清理都必须先完成资产身份审计；不得为了 clean 状态绕过用户确认或语义保全。项目收口不得默认使用 `git add .`，必须先按职责分桶处理 dirty worktree。

### P049｜稳定治理规则和动态项目状态混入长期 Prompt 或 Skill

- **状态**：已确认
- **问题与适用范围**：长期重复使用的 Prompt、项目 Skill、收口模板、交接流程或自动化任务同时承载稳定治理规则和当前窗口动态事实。

  适用：项目 Skill、Bridge Prompt、收口模板、交接流程和自动化任务。
- **可观察表现**：Prompt 或 Skill 过长；阶段变化后仍携带旧数字、分支、当前代码或下一步；旧状态可能被重新写回 Current。
- **根因与常见错误处理**：没有区分长期稳定的治理机制与当前窗口的动态事实。

  常见错误处理：将当前进度、数量、分支、commit、当前代码和下一步永久写进重复使用的 Skill 或固定模板。
- **正确处理**：Bridge Prompt 提取当前动态变化并生成一次性 Source Package；项目 Skill 只保存长期稳定的治理和文件职责。
- **验证与防复发**：检查永久 Skill 是否不包含会随阶段变化的项目状态；动态事实是否来自当前 Source Package 和仓库验证。

  防复发：会随阶段变化的事实不得写死在永久 Skill 中。

### P051｜规则未确认前编写生成代码固化 Working Assumption

- **状态**：已确认
- **问题与适用范围**：内容生成器、数据处理脚本、Notebook、Colab 和自动化流程在规则、映射、输入输出和 QA 未确认前开始实现。

  适用：内容生成器、数据处理脚本、Notebook、Colab 和自动化流程。
- **可观察表现**：生成脚本或 Notebook 编写后，规则、字段或验收条件发生变化，导致代码、输出和文档一起返工。
- **根因与常见错误处理**：执行顺序倒置，为尽快看到结果而先实现、后确认规则。

  常见错误处理：在事实来源、字段职责、匹配逻辑、输出边界和 QA 标准尚未确定时直接编写生成器。
- **正确处理**：先确认权威输入、规则、映射、输入输出和验收标准，再开始编写代码。
- **验证与防复发**：代码开始前检查是否已有明确的规则状态、输入输出契约和 QA 门禁。

  防复发：未满足实现开始条件时，只做规则和设计工作，不编写正式生成器。

### P061｜治理目录缺少独立 Git 边界导致门禁无法执行

- **状态**：已确认
- **问题与适用范围**：独立治理目录需要运行依赖 Git branch、HEAD、commit 和 clean worktree 的治理 Skill，但该目录自身没有 Git；适用于与其他仓库并列或包含多个独立嵌套仓库的治理目录。
- **可观察表现**：治理 Skill 无法确定该目录自身的 branch、HEAD、提交历史或 clean worktree，因而不能通过 Git gate；若在更高层总目录初始化 Git，又会把多个原本独立的仓库纳入同一边界。
- **根因与常见错误处理**：治理流程依赖可验证的本地版本边界，但目标治理目录未建立自身 Git；常见错误处理是在包含多个独立仓库的总根目录初始化一个总 Git，造成职责、状态和提交范围混杂。
- **正确处理**：仅为需要治理门禁的独立治理目录建立自身 Git 边界，保持其他独立仓库及更高层总目录不变；不得为满足门禁而在包含多个嵌套仓库的总根目录初始化总 Git。
- **验证与防复发**：从治理目录内核对 Git root 正好等于该目录，并确认 branch、HEAD、提交历史和 worktree 状态均可独立读取；再从更高层确认没有新增覆盖多个独立仓库的总 Git。

  防复发：建立需要 Git gate 的治理目录时，应同步确认其独立版本边界；Git root 必须与治理职责边界一致。

### P062｜运行标签和正常返回掩盖真实副作用或失败

- **状态**：已确认
- **问题与适用范围**：Notebook、脚本、配置同步器或批处理使用 `DRY_RUN`、Preview、written、inserted、updated、SUCCESS 等标签，或 Cell 正常结束，但标签和返回状态没有准确反映实际外部副作用与业务结果。
- **可观察表现**：标记为 Preview 或 Dry Run 的任务仍创建或修改工作表、字段映射、配置快照或 RunLog；摘要中的 written、inserted、updated 实际只是计划数量；异常被捕获后返回 FAILED 而不重新抛出，Notebook Cell 仍显示正常完成；phase 与实际写入行为不一致。
- **根因与常见错误处理**：只把 Dry Run 理解为“不调用主要业务 API”，或用函数是否抛异常、Cell 是否结束和一个总状态代替对各类副作用与部分失败的判断。

  常见错误处理：看到 `DRY_RUN=True` 就默认绝对只读；把 planned 数量命名为 written；只凭 Cell 的绿色完成状态宣布 Job 成功。
- **正确处理**：为每个运行模式明确列出是否调用业务 API、修改业务数据、修改配置、创建输出、写日志及是否完全只读。结果字段严格区分 planned、validated、written、succeeded、failed 和 partial success；捕获异常时仍必须让最终 Job 状态和可观察失败信号一致。
- **验证与防复发**：运行前后分别核对业务系统、业务表、配置表、输出对象和 RunLog；将摘要与真实差异逐项对账，并注入校验失败、执行失败和部分成功样例，确认进程状态、Job 状态、phase、计数和副作用一致。

  防复发：运行模式名称、Notebook 文案和返回字段都不是副作用证据；必须以实际差异和分层结果验收。

### P063｜阻断级质量检查发生在覆盖写入之后

- **状态**：已确认
- **问题与适用范围**：清洗、编译、同步或输出流程先覆盖正式结果，之后才检查标题、尺寸、标识、价格、字段关系或其他阻断级质量条件。
- **可观察表现**：任务最后打印错误或异常摘要，但不合格数据已经覆盖输出表或进入下游；操作者虽看见报告，却无法阻止本次坏写入。
- **根因与常见错误处理**：质量检查被实现为运行后报告，而不是写入前门禁；流程把“发现问题”和“阻止问题落盘”混为一件事。

  常见错误处理：先写入再依靠 warning 提醒；认为有最终 Validation Sheet 就足以保护正式输出。
- **正确处理**：执行顺序固定为 `validate → fail/preview → explicit apply`。阻断级错误必须在覆盖或真实写入前完成检查并停止；只有不影响本次输出有效性的普通 warning 可以在写入后汇总。
- **验证与防复发**：使用包含阻断错误的输入运行，确认正式目标字节、行数和关键值均未变化；修复输入并明确 Apply 后再验证写入和回读结果。

  防复发：任何覆盖或真实写入流程都必须能证明阻断级验证位于第一个不可逆或对外可见副作用之前。

### P064｜名称和注释与实际执行职责漂移

- **状态**：已确认
- **问题与适用范围**：Notebook 名、Python 文件名、Job 名、docstring、注释、界面文案或保存的运行输出与当前代码实际职责、版本和调用路径不一致。
- **可观察表现**：按文件名打开的模块实际执行另一类旧逻辑；Notebook 声称的 Job 与导入函数不一致；新旧副本分散在不同目录；当前代码与保存输出显示的版本或行为不同。
- **根因与常见错误处理**：历史复制、移动、改名或局部升级只更新了部分标识，调用路由和运行证据没有同步；交付方还可能把版本化下载名误当成正式模块名，擅自改变真实仓库路径或 import 身份；排查时又把名称或注释当成权威来源。

  常见错误处理：仅凭文件名、标题或注释判定模块职责；看到近似副本便自动删除、改名或重构；忽略真实仓库树、实际 import、调用方和运行路径。
- **正确处理**：先以真实仓库树、正式目录、模块名和 import 调用链确认权威身份，再综合核对 docstring、调用方、Job 路由、模块版本、Git history 和真实运行结果；版本化下载名只用于交付与防缓存，不得反向改变正式路径或模块名。先记录当前行为、声称行为、计划行为和历史差异，再决定是否迁移或重构。
- **验证与防复发**：从正式入口执行一次可识别的最小流程，打印实际模块路径和版本，追踪到被调用函数及输出；确认路由、名称、文档和运行证据一致后，才把该实现认定为 Current。

  防复发：名称是检索线索，不是权威性证据；模块职责必须由实际调用链和运行证据共同确认。

### P065｜Warning 噪音过大导致异常失去治理价值

- **状态**：已确认
- **问题与适用范围**：Prepare、校验、同步或批处理一次产生大量重复 warning，且没有分类、严重性或受影响对象汇总。
- **可观察表现**：一次运行出现成千上万条近似 warning；操作者无法识别阻断问题、真正需要处理的对象和可忽略噪音；部分应计入 warning 的 unresolved 问题又可能只出现在明细中。
- **根因与常见错误处理**：逐输入行直接输出原始消息，没有稳定 warning code、去重聚合、严重性分级和展示上限；只统计消息条数，不统计受影响业务对象。

  常见错误处理：逐条刷屏；简单截断全部 warning 而丢失类别；只给总数而不提供可修复来源和代表样例。
- **正确处理**：为 warning 定义稳定 code 和阻断、重要、可忽略等级；按类别聚合，每类保留前 N 个代表样例，分别统计 warning 事件数和受影响对象数，并指出可修复规则来源。未解决的图片、Schema 或引用问题必须进入对应计数和摘要。
- **验证与防复发**：用含重复问题和多种严重性的样例运行，确认总量、分类量、受影响对象数和明细可相互对账；摘要能让操作者直接定位阻断项，完整明细仍可追溯且控制台不逐条刷屏。

  防复发：warning 输出的目标是支持决策和修复，不是证明系统发现了很多问题。

### P066｜辅助 Registry 失败阻断主要业务任务

- **状态**：已确认
- **问题与适用范围**：Notebook 在主要同步、导出、批处理或写入任务之前执行可选 Registry 登记，登记异常与主任务共用阻断路径。
- **可观察表现**：目标登记记录不存在时抛出异常，主要业务逻辑尚未开始便中止；操作者看到的是辅助登记错误，而不是业务任务结果。
- **根因与常见错误处理**：把辅助登记默认启用，并将“未登记、重复登记或登记服务异常”视为主要任务的强制前置条件，没有区分治理辅助功能与业务正确性门禁。

  常见错误处理：要求 Registry 自动追加缺失记录；捕获所有异常但静默忽略；或把所有登记错误永久设为不可阻断，取消显式严格模式。
- **正确处理**：Registry 默认关闭。启用时按稳定逻辑唯一键查找已有记录，只允许唯一匹配时定向更新；零条或多条匹配均明确 warning，不自动追加或任意修改。默认 `REGISTRY_STRICT=False` 时隔离登记异常并继续主任务；只有操作者显式启用 strict，才让登记失败中止运行。
- **验证与防复发**：分别测试关闭 Registry、唯一匹配、零匹配、多匹配和登记服务异常；非 strict 模式下主任务均能继续且摘要包含 Registry warning，strict 模式下才在业务副作用前明确失败；所有场景都不会自动新增或误改记录。

  防复发：辅助治理功能必须有独立开关、独立结果和明确 strict 边界，不能默认成为业务任务的单点阻断器。

### P067｜长任务缺少运行中反馈而无法判断真实状态

- **状态**：已确认
- **问题与适用范围**：Notebook、脚本、批处理、同步、导出或 API 写入任务持续较长时间，但运行期间没有阶段进度、批次统计或 heartbeat。
- **可观察表现**：任务只在结束时输出结果；运行中操作者无法区分正常处理、卡死、限流等待、认证阻塞或 Kernel 失效，也无法判断已完成量和失败范围。
- **根因与常见错误处理**：实现只关注最终返回值，没有把可观察性作为长任务接口的一部分；或为显示“详细进度”逐对象刷屏，反而淹没 warning、error 和重试信号。
- **正确处理**：按阶段切换、每批或每 N 条更新、单行动态进度或周期性 heartbeat 提供适度反馈；报告当前阶段、完成量/总量、当前对象或批次、成功/失败/跳过、warning/error、重试和限流等待，信息足够时给出 ETA 或剩余量判断。错误和重试立即提示，重复信息聚合，结束时输出可对账摘要。
- **验证与防复发**：用包含正常处理、限流重试、单批失败和长等待的样例运行；确认在任务结束前能持续看到状态变化，控制台不会逐行刷屏，最终摘要与批次及对象级结果一致。

  防复发：长任务的阶段进度、heartbeat 和最终汇总应作为验收项，不能把“最终有结果”视为足够的运行可观察性。

### P068｜把 Clone 或 Publish 当成连接两个既有仓库

- **状态**：已确认
- **问题与适用范围**：已有非空本地 Git 仓库需要连接到一个已经存在的远端仓库时，误用 Clone 或托管客户端的 Publish 功能。
- **可观察表现**：Clone 拒绝使用包含文件的目标目录；Publish 提示同名远端仓库已经存在；本地文件和提交历史面临被另建副本或绕开的风险。
- **根因与常见错误处理**：Clone 的语义是创建新的工作副本，Publish 的语义通常是新建远端；两者都不是为已有本地仓库绑定已有远端而设计。

  常见错误处理：为通过 Clone 而清空或覆盖现有目录；反复 Publish；或在已有同名 remote 时重复执行 `remote add`。
- **正确处理**：先把现有目录作为本地仓库加入客户端，核对 Git root、工作树和历史，再检查或配置现有 remote；连接完成后分别确认当前分支、远端分支和 upstream。若两端历史关系异常，按 P070 独立处理，不把连接成功等同于可以直接同步。
- **验证与防复发**：确认 Git root 仍是原本的本地目录，原提交历史可读，remote URL 指向预期仓库，当前分支 upstream 明确；在执行任何 Pull、Merge 或 Push 前检查本地与远端提交关系。

  防复发：连接“已有本地仓库 + 已有远端仓库”时，先核对和绑定 remote；Clone 与 Publish 都不得作为默认连接动作。

### P069｜混淆桌面客户端登录与终端 Git HTTPS 认证

- **状态**：已确认
- **问题与适用范围**：Git 托管桌面客户端已经通过浏览器或 OAuth 登录，但同一机器上的终端 Git 通过 HTTPS 访问远端。
- **可观察表现**：桌面客户端可以 Fetch、Pull 或 Push，终端 `git fetch` 仍要求 Username 和 Password；输入账户登录密码后提示不支持密码认证。
- **根因与常见错误处理**：桌面客户端可能使用自身 OAuth 与凭据存储，终端 Git 则依赖独立的 credential helper、访问令牌或 SSH 配置；客户端登录状态不会自动证明终端认证已配置。

  常见错误处理：在终端密码提示中输入账户登录密码；因为客户端可用就反复重试终端命令；或在没有验证凭据来源时认为 remote 配置错误。
- **正确处理**：先区分桌面客户端与终端的认证路径。未配置终端认证时，不输入账户密码；选择一个明确入口完成联网操作，例如继续使用已认证的桌面客户端，或另行配置受控的 credential helper、访问令牌或 SSH。终端仍可承担不联网的本地检查和分支操作。
- **验证与防复发**：分别从实际计划使用的客户端和终端执行最小远端读取，确认二者各自是否具备认证；日志和文档只记录认证方式与失败类别，不记录凭据值。

  防复发：把每个 Git 客户端的认证视为独立能力；不能用一个客户端已登录推断其他 Git 入口也已认证。

### P070｜把无共同祖先的本地与远端历史当成普通同步

- **状态**：已确认
- **问题与适用范围**：本地仓库与远端仓库分别初始化，双方都有需要判断或保留的提交历史，但没有共同祖先。
- **可观察表现**：双方各有独有提交，`merge-base` 找不到共同祖先，托管平台提示提交历史完全不同；即使当前文件内容相近，也不能正常按普通快进关系解释。
- **根因与常见错误处理**：文件树相似不代表提交图相连；把内容一致误当成历史一致，会让普通 Pull、Merge 或覆盖式 Push 引入无意义合并或丢失需要保留的审计历史。

  常见错误处理：未经审计直接 Pull、Merge 或 Force Push；仅比较当前文件内容；在没有保留目标历史和回退路径时替换默认分支。
- **正确处理**：先只读比较双方引用、独有提交和 `merge-base`，明确哪套历史必须成为最终权威。需要用本地审计历史替换远端主线时，先把目标历史发布为独立临时远端分支，验证后再切换默认分支、处置旧主分支、重命名并重新绑定 upstream；每一步都保持目标历史可见且可回退。其他历史取舍必须按实际保留目标单独设计，不能机械套用覆盖流程。
- **验证与防复发**：最终核对本地 HEAD、目标远端分支和 upstream 指向同一提交，并检查默认分支、远端引用和工作树符合预期；文件内容校验只能作为补充，不能替代提交图验证。

  防复发：连接两个已有仓库后必须先检查共同祖先和双方独有提交；没有共同祖先时，先做历史保全方案，再执行任何改变远端主线的操作。

### P071｜交互式认证中断批量 Git 命令并留下部分执行状态

- **状态**：已确认
- **问题与适用范围**：连续粘贴或串行执行一组同时包含本地 Git 操作和联网 Git 操作的命令，而联网步骤可能进入交互式认证。
- **可观察表现**：前面的分支删除、重命名或配置修改已经成功，后续 Fetch、状态检查或引用更新却停在 Username/Password 提示或根本没有执行；工作区处于不同于原计划和最终目标的中间状态。
- **根因与常见错误处理**：交互式认证暂停了命令序列，但操作者仍把整组命令当成一个原子操作，没有逐项确认哪些步骤已经产生副作用。

  常见错误处理：认证失败后重新粘贴整组命令；假设所有步骤都失败或都成功；不核对已经改变的本地引用便改用其他客户端继续。
- **正确处理**：遇到意外认证提示时先中止当前联网步骤；把命令拆成可验证的小步，区分已经成功的本地操作、失败的联网操作和尚未执行的后续操作。联网同步交给已确认认证的入口后，再用不联网命令逐项重建和验证当前状态。
- **验证与防复发**：检查当前分支、所有相关本地引用、remote、upstream 和工作树，再单独验证远端读取；只有每个计划步骤都有明确结果时才宣布整组操作完成。

  防复发：可能触发认证的联网 Git 命令不得与不可忽略的本地变更命令作为未经检查的批量序列执行。

### P072｜无关 dotenv 空字段阻断目标 Secret 解析

- **状态**：已确认
- **问题与适用范围**：共享 Secret Resolver 为当前项目或服务查找一个目标 Secret 时，会扫描同一受控范围内含有其他服务配置的 dotenv 文件。
- **可观察表现**：请求的目标 Secret 已存在且有效，Resolver 却因为无关文件中的 Optional 字段为空而抛出 Secret 合同错误，导致认证或后续业务流程无法开始。
- **根因与常见错误处理**：Resolver 在确认目标候选 key 之前，对每个扫描到的 dotenv 文件执行“任何空值都 fail-fast”的全文件严格解析，把无关 Optional 字段错误升级为当前请求失败。

  常见错误处理：要求所有无关 Optional 字段填入占位值；放宽全部校验而同时掩盖目标值为空、重复 key 或多文件歧义；或绕过共享 Resolver 在业务代码中直接读取 Secret。
- **正确处理**：dotenv 解析允许无关字段为空；只有当前请求的候选 Secret key 为空时才报错。目标值缺失、重复 key 和多文件歧义继续 fail-fast，并保持项目边界和来源优先级明确。
- **验证与防复发**：单元测试同时覆盖无关 Optional 空字段、目标字段为空、目标缺失、重复 key 和多来源歧义；修复后还要从真实认证边界完成一次目标 Secret 解析和最小业务调用，证明不是仅让解析器停止报错。

  防复发：Secret 合同校验应围绕当前请求的候选 key 执行；不能让无关 Optional 配置阻断目标解析，也不能为容忍空值而削弱目标歧义和缺失检查。

### P073｜bracketed-paste 控制字符污染 shell 命令

- **状态**：已确认
- **问题与适用范围**：向交互式 shell 粘贴安装、验证或其他命令时，终端的 bracketed-paste 控制字符被当作普通命令文本输入。
- **可观察表现**：命令开头出现 `^[[200~` 等控制字符，zsh 报 `bad pattern` 或等价解析错误；后续检查显示预期安装或操作并未发生。
- **根因与常见错误处理**：粘贴边界控制序列没有被终端正确消费，污染了实际提交给 shell 的文本。

  常见错误处理：只看到后续提示便假设命令已经执行；在污染的输入行上继续补字符或重试；没有重新验证预期副作用。
- **正确处理**：取消当前输入，重新使用不带控制字符的干净单行命令或手动输入；将安装、执行与验证拆开，避免污染命令造成的失败被后续输出掩盖。
- **验证与防复发**：确认重新输入的命令文本开头和结尾没有控制字符，检查退出状态，再单独验证预期依赖、文件或状态确实存在。

  防复发：看到 `^[[200~`、`bad pattern` 或异常命令前缀时，应先判定本次命令未可靠执行，清理输入后重跑并验证，不把它误诊为依赖或业务逻辑问题。

### P074｜把作用域内字段键误当成全局唯一键

- **状态**：已确认
- **问题与适用范围**：配置驱动的字段注册表允许不同 owner、entity 或命名空间复用同一语义键，但读取器把该键当成全表唯一身份；适用于字段字典、Schema Registry、元数据映射和多实体配置。
- **可观察表现**：同一字段键在不同作用域下合法出现时被错误报告为重复；或单层字典后写覆盖先写，导致解析和回查命中错误实体的定义。
- **根因与常见错误处理**：实现凭字段名建立单层索引并自行施加全局唯一约束，没有使用 Schema 声明的真实主键或复合键，也没有在查询时保留 owner/entity scope。

  常见错误处理：禁止不同作用域复用语义键；仅取消重复检查却继续使用会覆盖的单层字典；或依赖调用方事后猜测所属实体。
- **正确处理**：以 Schema 声明的主键或复合键作为记录身份，并按需要建立 `by_primary_id`、`by_semantic_key` 和 `by_owner_and_key` 等不同用途的索引；所有解析和回查显式携带作用域。分别校验重复主键、复合键组成部分不一致和本应唯一的作用域内重复，不能把某个组成字段自动升级为全局唯一键。
- **验证与防复发**：测试同一语义键跨作用域合法重复、同一复合主键非法重复、主键组成部分冲突以及 owner-aware lookup；确认每种索引的返回基数和歧义行为与 Schema 合同一致。

  防复发：字段身份和查询作用域必须来自声明的 Schema；语义键可搜索不等于它能单独充当全局主键。

### P075｜Runtime 刷新被解释器生成物误判为源码修改

- **状态**：已确认
- **问题与适用范围**：通过 Git clone 维护 Runtime 执行副本，并在刷新前用 dirty guard 防止覆盖人工修改时，解释器或 Notebook 自动生成的缓存文件进入工作树。
- **可观察表现**：源码没有人工变化，Runtime 刷新仍因未跟踪的字节码、缓存目录或 Notebook checkpoint 被拒绝；简单放宽 guard 后又可能让真实未知文件被覆盖。
- **根因与常见错误处理**：刷新逻辑直接把所有 `git status` 输出视为同一种人工修改，没有区分可明确重建的生成物和身份未知的源码或资产。

  常见错误处理：遇到 dirty 状态便删除全部未跟踪文件；将整个 Runtime 强制重置；或完全忽略 untracked 内容继续覆盖。
- **正确处理**：刷新前只识别和清理严格白名单中的安全生成物，例如 Python 字节码缓存、Notebook checkpoint 和常见系统元数据；设置不写字节码的运行选项以减少复发；清理后重新检查 Git 状态。任何白名单外的 tracked 或 untracked 内容仍必须阻止覆盖并进入定向判断，遵守 P047 的资产身份边界。
- **验证与防复发**：分别注入白名单生成物和未知源码文件；确认前者可被定向清理且刷新继续，后者仍触发保护。刷新后再次检查工作树，并验证实际加载路径和版本符合 Runtime 合同。

  防复发：dirty guard 可以自动处置的只能是明确、有限且可重建的生成物；不得用“保持可刷新”作为批量删除未知文件的理由。

### P076｜只用返回列表长度判断工作表是否为空

- **状态**：已确认
- **问题与适用范围**：表格初始化或表头迁移逻辑依据 values 返回列表是否存在判断工作表为空，而不同 API 或封装可能用空列表、空行、空字符串矩阵或带空格的矩阵表示视觉空表。
- **可观察表现**：已存在但完全空白的工作表被判定为表头不匹配，错误信息显示空表头或空行；初始化没有发生，任务在首次写入前中止。
- **根因与常见错误处理**：代码只判断 `values == []` 或列表长度，没有规范化单元格并检查整个矩阵是否存在非空内容；随后把第一行直接当作实际表头比较。

  常见错误处理：只为某一种空返回形态增加特例；无条件覆盖所有表头不匹配的工作表；或把空白矩阵当成需要人工清理的损坏数据。
- **正确处理**：先对整个返回矩阵执行 normalized non-empty 检查，将不存在、空列表、空行和纯空白矩阵统一视为空表并初始化；精确匹配正式表头时复用；仅对已声明的旧标准表头执行有边界的扩展迁移；只在存在非空且不兼容内容时拒绝覆盖。
- **验证与防复发**：测试工作表缺失、`[]`、`[[]]`、多行空字符串、带空格矩阵、正确表头、可迁移旧表头和非空不兼容内容；确认空表会初始化、兼容表头会复用或按边界迁移，而未知内容保持不变并明确失败。

  防复发：工作表是否为空应按规范化后的内容语义判断，不能用 API 返回容器的形状代替。


## 7. 网络与运行环境

### P028｜TUN 与系统代理双开导致超时或回环

- **状态**：已确认
- **问题与适用范围**：Clash Verge 等工具同时开启 TUN 和系统代理，或前后两层代理路由互相承接不清。
- **可观察表现**：开启虚拟网卡后所有请求 timeout；关闭后立即恢复；重装软件通常无效。
- **根因与常见错误处理**：流量可能被重复接管、发生代理回环，或 DNS/路由同时被两套机制改写。
- **正确处理**：先采用单一接管方式排查：开 TUN 时关闭系统代理；确认 DNS、路由和 bypass 后，再逐层验证出口代理。不要在路径尚未验证时同时叠加多种接管方式。
- **验证与防复发**：依次测试直连、TUN 单开、出口层单独连通和最终出口 IP；每增加一层都确认 DNS 与目标应用仍正常。

### P029｜只验证浏览器出口，没有验证 Codex/CLI 实际出口

- **状态**：已废弃（资格不足）
- **说明**：原记录包含模板占位内容，且没有证明事件实际发生或建立根因，因此不作为正式通用 Pitfall。P029 编号永久保留，不再复用；如以后取得充分证据，应在本编号原位重建完整条目。

  ## 9. 新条目模板

  复制以下结构追加新问题：

  ``` markdown
  ### PXXX｜简短问题名称 如何证明修复真正有效，而不只是代码不再报错。
  ```

  ## 10. 文档与知识资产管理

## 8. 新条目模板

```markdown
### PXXX｜简短问题名称

- **状态**：已确认 / 部分确认 / 待验证 / 已废弃 / 已替代
- **问题与适用范围**：问题在什么系统、步骤、输入或边界下发生，以及可跨任务复用的范围。
- **可观察表现**：关键错误码、错误文本、业务异常或可重复观察到的结果。
- **根因与常见错误处理**：已经建立的原因，以及曾经无效、会掩盖问题或扩大风险的处理方式。
- **正确处理**：以后应采用的可执行处理方式。
- **验证与防复发**：怎样证明修复真正有效，以及应固定进入流程、代码或验收的最小规则。
```
