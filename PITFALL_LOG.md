# 踩坑日志

> 用途：记录已经遇到、容易复发、值得跨任务复用的故障与修复经验。
>
> 状态：Current 最后更新：2026-07-19

## 1. 使用规则

-   本文按"问题类型"组织，不按聊天日期堆叠流水账。
-   每条至少包含：场景、错误表现、错在哪里、正确做法、验收方式。
-   已确认的根因写"已确认"；仍在调查的内容必须写"待验证"。
-   一次性 ID、Secret
    值和大段原始日志不进入本文；必要时只保留错误码、关键错误文本和定位线索。
-   同一个问题再次出现时更新原条目，不重复新增近似条目。
-   项目当前状态、正式文件和下一步属于交接文档，不属于本文。

------------------------------------------------------------------------

## 2. Google 身份认证、Drive 与 Sheets

### P001｜Service Account 无法在 My Drive 创建文件

-   状态：已确认。
-   场景：Colab 使用 Service Account 在 Google Drive 中新建
    Spreadsheet、备份文件或执行需要占用个人 Drive 配额的操作。
-   错误表现：`403 storageQuotaExceeded`；即使 Service Account
    能读取或更新已共享文件，也可能无法创建新文件。
-   错在哪里：把"拥有 API 权限"和"拥有可用 Drive
    存储配额"当成了同一件事。Service Account 通常没有个人 My Drive
    配额。
-   正确做法：日常读取和更新已共享文件可继续使用 Service
    Account；需要新建、复制或备份时，使用用户
    OAuth（`COLAB_USER`），或把目标放在允许 Service Account 操作的
    Shared Drive。
-   验收方式：使用目标身份实际创建一个测试文件，并确认文件位于预期文件夹且所有者/权限正确；不能只以认证成功作为验收。

### P002｜授权弹窗运行到中途才出现

-   状态：已确认的工作流问题。
-   场景：Notebook 已完成大量下载或计算后，写入阶段才触发 Google OAuth。
-   错误表现：用户以为流程可以无人值守，运行到一半突然要求授权；错过弹窗后任务中断或超时。
-   错在哪里：认证被藏在业务流程内部，没有在运行开始时完成。
-   正确做法：Cell 1 只放 Controller；如果必须 OAuth，授权 Cell 必须紧接
    Cell 1。之后所有 Cell 复用已建立的凭据，不再次触发交互授权。
-   验收方式：从全新 Runtime 自上而下运行，确认所有必要弹窗都在 Cell 1
    后立即出现，后续长任务不再要求人工介入。

### P003｜把 httplib2 的 timeout 警告当成任务失败

-   状态：已确认。
-   场景：使用 Google 官方认证/客户端库访问 Drive 或 Sheets。
-   错误表现：出现
    `httplib2 transport does not support per-request timeout`，但后续仍能看到扫描心跳、读取进度或正常结果。
-   错在哪里：仅凭 WARNING
    判断任务已经失败，或者反过来完全忽略真正的超时风险。
-   正确做法：先看是否持续输出心跳、行数和当前处理对象。该 WARNING
    本身不等于失败；若确实需要超时控制，应在构造底层 `httplib2.Http`
    时统一设置，而不是给单次请求传不支持的 timeout。
-   验收方式：确认请求继续推进并最终输出 `[SUMMARY]`；若修改了
    timeout，使用可控的慢请求验证确实会按设定中止。

### P004｜写入范围超过 Google Sheet 网格

-   状态：已确认。
-   场景：通过 gspread 或 Sheets API 批量写入比当前 Sheet
    行列数更大的区域。
-   错误表现：HTTP 400，常见文本为 `Range exceeds grid limits`；动态区块增加时，也可能在本可扩容的 `columnCount` 检查处提前失败。
-   错在哪里：只按 DataFrame 生成写入 Range，或把 capacity validation 放在 resize 之前，没有按动态布局先计算并扩展目标 Sheet 的 `rowCount` 与 `columnCount`。
-   正确做法：动态布局遵循 `calculate → resize → validate`：先计算包含表头和全部动态区块的所需行列数，不足时 resize 或通过 `batchUpdate` 扩展网格，再校验容量并执行 values update。若是全量替换，先清空目标区域但不要误删 Sheet。
-   验收方式：读取 Sheet
    properties，确认动态规模增加时会先扩容，网格尺寸不小于计划写入范围；写入后核对实际行列数和最后一个目标单元格。

### P005｜写完以后回读比对全部 mismatch

-   状态：部分确认，具体 Notebook 根因需按版本复查。
-   场景：Google Ads Overview 批量格式化或写值后，对多个 Range
    做严格回读比较。
-   错误表现：多个写入范围同时进入
    `Overview post-write mismatch`，即使表面上部分内容已经写入。
-   错在哪里：可能把请求体中的 Python 表示与 Sheets
    回读后的规范化表示直接逐对象比较；日期、数字、空值、公式、合并单元格及格式字段都可能被
    API 规范化。若所有 Range
    同时失败，也可能是比较层取错字段或取错维度，不能直接重试写入掩盖问题。
-   正确做法：分别校验 values 与
    formatting；比较前统一空值、数字、日期和二维尺寸。只比较业务上必须一致的字段，并在错误中输出首个具体差异的坐标、期望值和实际值。
-   验收方式：用一个小 Range
    建立可重复测试，故意制造一个单元格差异，确认校验只报告该位置；在当前
    Overview Notebook 中仍需结合实际返回结构复查根因。

### P006｜默认新建 Tab，导致同类输出越来越多

-   状态：已确认的维护问题。
-   场景：每次运行都以时间戳或"new"名称创建新 Sheet Tab。
-   错误表现：Spreadsheet 中出现大量近似
    Tab，引用关系混乱，用户无法判断哪个是正式结果。
-   错在哪里：把避免覆盖风险等同于不断创建新目标，却没有稳定的正式输出入口。
-   正确做法：正式 Tab 名固定；默认清空并全量写入同名
    Tab。只有用户明确要求保留快照时才创建带时间版本的备份。
-   验收方式：连续运行两次后，目标 Tab 数量不增加；正式 Tab
    内容更新，引用仍指向同一位置。

------------------------------------------------------------------------

## 3. Pandas、数据结构与汇总

### P007｜`TypeError: unhashable type: 'numpy.ndarray'`

-   状态：已确认错误类型；触发列需按当前运行数据定位。
-   场景：对包含 `numpy.ndarray`、list 或其他嵌套对象的 Series 执行
    `factorize`、`Categorical`、排序、去重、分组或设为索引。
-   错误表现：Pandas 在 `factorize()` 或 `Categorical` 内部报
    `TypeError: unhashable type: 'numpy.ndarray'`。
-   错在哪里：本应为标量的单元格混入了 ndarray/list。Pandas
    的分类、哈希和去重操作要求元素可哈希。
-   正确做法：先定位对象列及异常值类型；根据业务含义把单元素数组解包为标量、多元素数组规范化为
    tuple 或稳定字符串。不要粗暴对整张表
    `astype(str)`，否则会掩盖数据结构错误并破坏数值与空值语义。
-   验收方式：输出目标列的类型分布；确认规范化后不存在
    ndarray/list，再运行原来的
    categorical/sort/groupby，并核对行数与排序结果。

### P008｜跨月 Fact 表重复累计同一个 Variant Base

-   状态：已确认。
-   场景：价格变更分析把月度事实表与 Variant Base 属性直接汇总。
-   错误表现：运行 6 个月后，同一 Variant Base 的
    Count、Cost、Price、调前价格和调后价格被累计 6 次。
-   错在哪里：把"主体属性"与"期间指标"都按月度行求和，没有区分静态粒度和时间累计粒度。
-   正确做法：Count、Cost、Price、调前/调后价格按 Variant Base
    只取一次；Net Sales、Eligible 等期间指标按 Controller
    完整日期范围汇总；状态以 Controller 结束日为准。
-   验收方式：抽取一个跨多月存在的 Variant
    Base，确认静态字段只计一次，期间销售指标等于各月之和；整体
    `nunique(variant_base)` 与 Count 口径一致。

### P009｜多次调价记录让 Variant Base 重复

-   状态：已确认。
-   场景：一个 Variant Base 在价格日志中存在多次调价记录，直接 join
    到主体表。
-   错误表现：同一 Variant Base 出现多行，后续销售与广告数据跟着重复。
-   错在哪里：没有先把价格日志压缩到目标粒度就参与 join。
-   正确做法：按已确认规则从多次调价中提取首/末信息；主体表最终保持每个
    Variant Base
    一行。若业务只需要一个主要值，应明确采用最大调价日期或其他已确认选择规则。
-   验收方式：join 前后分别检查 `variant_base`
    唯一性；合并后行数不得无解释增长。

### P010｜合并表后行数突然膨胀

-   状态：通用已确认。
-   场景：将 Shopify、广告、价格日志或分类映射等多张表合并。
-   错误表现：结果行数成倍增加，汇总金额异常变大，但代码没有报错。
-   错在哪里：预期 1:1 或 N:1 的键在右表并不唯一，形成 N:N join。
-   正确做法：合并前分别检查键的重复数；使用 Pandas
    `merge(..., validate='one_to_one'/'many_to_one')`
    明确关系。需要聚合或选最新记录时，在 merge 之前完成。
-   验收方式：记录 merge 前后行数与唯一键数；金额类字段做控制总额对账。

### P011｜用字符串化解决所有数据类型问题

-   状态：通用已确认。
-   场景：遇到 JSON、数组、日期、Decimal
    或空值导致写入/排序失败时，直接对整表 `astype(str)`。
-   错误表现：代码暂时不报错，但出现字符串 `nan`、日期无法排序、数字在
    Sheet 中成为文本、数组比较失真。
-   错在哪里：用展示层转换掩盖了数据模型问题。
-   正确做法：按列和业务语义规范化；数字保持数值、日期保持 datetime 或
    ISO
    字符串、空值统一为写入端可接受的空值、嵌套对象按明确规则展开或序列化。
-   验收方式：检查关键列 dtype、Sheet
    中数字格式、空值数量和日期排序；禁止仅以"能写进去"作为验收。

------------------------------------------------------------------------

## 4. Google Ads 数据与校验

### P012｜把近期重新归因当成历史数据损坏

-   状态：已确认。
-   场景：本地 Google Ads 基座与 API 对账，尤其包含最近数天或近 30
    天数据。
-   错误表现：Cost、Conversions、Conversion Value 等出现小幅差异；严格的
    `差异 > 0 即 FAIL` 导致频繁失败。
-   错在哪里：忽略 Google Ads
    近期转化回补和重新归因，同时也不能因此对所有历史差异无限放宽。
-   正确做法：保留
    `API_RECONCILIATION_STRICT_MODE`。严格模式下任意差异按规则失败；非严格模式只允许近期、可解释且在明确阈值内的差异降级为
    WARNING，历史严重偏差仍必须 FAIL。
-   验收方式：报告中分开列出近期容差、历史偏差和实际阈值；用一条超阈值历史差异确认非严格模式仍会失败。

### P013｜混淆实体存在与期间活动状态

-   状态：已确认的范围问题。
-   场景：构建广告数据基座或期间报表时，用当前启用状态或近期活动判断 Campaign、Ad Group 等实体是否存在。
-   错误表现：已暂停实体的历史事实消失，或真实实体因期间无活动被替换成 `No Data` 等占位身份。
-   错在哪里：把 `entity existence` 与 `period activity` 合并判断，并把当前状态过滤错误地应用到历史事实或展示身份。
-   正确做法：实体身份来自计划、配置或完整实体集合；Data Foundation 默认同步 ALL 状态。期间无活动只影响期间数值，状态只作为维度或筛选项，不得删除历史事实或改写实体身份。
-   验收方式：抽查一个历史有数据但当前暂停的实体，以及一个期间无活动但仍在计划集合中的实体；前者历史事实仍存在，后者保留真实身份且期间值按规则为空或为零。

### P014｜把 ITEM_DAILY 当成 Asset Group 粒度

-   状态：已确认的 API 粒度限制。
-   场景：Google Ads Shopping/PMax 商品日报按 `product_item_id`
    获取数据。
-   错误表现：期望看到 Asset Group，但对应字段为空，容易误判为同步失败。
-   错在哪里：输出粒度与期望维度不一致；`product_item_id`
    粒度结果不保证提供可用的 Asset Group 归属。
-   正确做法：在字段文档中明确 ITEM_DAILY 的实际粒度和限制。需要 Asset
    Group
    分析时使用能稳定提供该维度的独立查询/数据集，不用空值强行补造。
-   验收方式：检查查询资源与 segments；在输出说明中明确 Asset Group
    是否可用，空值不计为同步错误。

### P015｜API 对账只看总额，不看日期与实体

-   状态：通用已确认。
-   场景：本地数据与 Google Ads API 总 Cost 恰好相同，但部分日期或
    Campaign 错位。
-   错误表现：总额校验通过，Overview 仍出现缺行、重复或归类错误。
-   错在哪里：总额相等不能证明数据完整；错误可能互相抵消。
-   正确做法：至少分层校验总额、日期、账户、Campaign
    和数据集主键；对关键指标同时比较行数、唯一键和汇总值。
-   验收方式：故意交换两天的数据，确认总额测试虽然通过，但日期粒度校验会失败。

------------------------------------------------------------------------

## 5. Shopify、商品与 Meta API

### P016｜`metafieldsSet` 返回 Access denied

-   状态：已确认。
-   场景：通过 Shopify GraphQL 批量写入 Product 或 Variant Metafield。
-   错误表现：GraphQL 返回
    `Access denied`；其他商品读取或更新操作可能仍然正常。
-   错在哪里：把"应用能访问商品"理解为"应用拥有全部 Metafield
    写权限"，或没有检查目标 owner type 与 token scope。
-   正确做法：检查 Admin API access scopes、应用安装后的实际授权、目标
    owner ID/type 和 metafield definition
    权限。权限未补齐前，明确标记该功能不可用，不能吞掉错误假装写入成功。
-   验收方式：先对一个测试对象写入并立即 query 回读；确认
    namespace、key、type、value 和 owner 均正确后再批量运行。

### P017｜ShopifyQL `parseErrors`

-   状态：已确认错误类型，具体语法需按查询版本复核。
-   场景：通过 ShopifyQL 获取销售或商品分析数据。
-   错误表现：响应包含 `parseErrors`，可能没有标准 HTTP 失败码。
-   错在哪里：只检查 HTTP
    200，没有检查响应体中的语法错误；或沿用已变更/不支持的字段、日期和查询语法。
-   正确做法：对响应同时检查 transport error、GraphQL/ShopifyQL error
    和业务数据是否为空。记录查询阶段与安全的查询片段，按当前 ShopifyQL
    语法逐段缩小定位。
-   验收方式：先运行最小查询，再逐段加入字段和过滤条件；最终确认
    `parseErrors` 为空且返回行数合理。

### P018｜Meta API 400 被当成可以自动重试的网络错误

-   状态：已确认错误码类型；具体请求根因需看当次响应。
-   场景：Meta API 返回 HTTP 400，曾见 `code=2, subcode=1504044`。
-   错误表现：无脑重试仍持续失败，或吞掉错误后留下部分结果。
-   错在哪里：HTTP 400
    往往是请求、对象状态、权限或平台侧业务限制，不等同于瞬时网络超时；仅凭
    code/subcode 不能在没有响应上下文时下唯一结论。
-   正确做法：保留 request stage、object
    ID、code、subcode、`error_user_title`/`error_user_msg`
    等非敏感上下文；先判断是否可重试。只有明确的瞬时错误才指数退避，参数或权限错误应立即停止并修正。
-   验收方式：修正后对单个对象重放请求并成功，再恢复批量；批量摘要必须报告成功、失败和跳过数量。

### P019｜SPU/Wholesale 图片合并时顺序丢失或重复

-   状态：已确认的业务规则。
-   场景：从 Each Box、SPU 或源商品合并产品图与 Variant 图。
-   错误表现：主图顺序变化、同一 URL
    重复、较长的有效图片列表被较短列表覆盖。
-   错在哪里：使用无序 set 去重，或简单以后一份列表覆盖前一份。
-   正确做法：按已确认来源优先级顺序合并并稳定去重；保留最长有效列表；分别处理
    `product.images_urls` 的 JSON 顺序与 `variant.image_url`。
-   验收方式：用包含重复 URL、不同长度和 Variant
    图的样本，核对最终顺序、数量和主图。

### P020｜批量 SEO 文案脱离商品标题事实

-   状态：已确认的内容规则。
-   场景：批量生成 Product Title、SEO 或 Feed 文案。
-   错误表现：出现"选择尺寸后加入购物车"等非产品事实提示，或生成标题中不存在的规格和功能。
-   错在哪里：模型用通用电商话术补充事实，而没有把产品标题和已确认字段作为事实边界。
-   正确做法：产品标题与结构化字段是最终事实；禁止补造规格、兼容性、服务或购买流程提示。无法确认时保守省略。
-   验收方式：抽查生成字段中的尺寸、材质、兼容性和功能，确保均能回溯到输入字段。

------------------------------------------------------------------------

## 6. Notebook、文件和交付方式

### P021｜所有流程塞进一个超大 Colab

-   状态：已确认的维护偏好。
-   场景：把创建、同步、清洗、校验、构建 Overview 和写入全部放入一个
    Notebook。
-   错误表现：任一阶段出错都难以定位，修改局部逻辑需要理解整本
    Notebook，重新运行成本高。
-   错在哪里：以"文件少"为目标，牺牲了职责边界和可维护性。
-   正确做法：按清晰 Job 拆为多个职责单一的 Notebook，例如 Create → Sync
    → Validate → Build；共享规则通过 Console、配置或明确公共模块复用。
-   验收方式：每本 Notebook 都有独立
    Controller、输入输出、运行摘要和验收条件；可以单独重跑而不依赖手工隐藏状态。

### P022｜每次修改都生成"最新版_v2_final_final"

-   状态：已确认的版本管理问题。
-   场景：同一 Notebook 每次修改都创建一个模糊命名副本。
-   错误表现：目录中存在多个看似最新的文件，无法判断正式入口，链接和文档持续失效；重命名后还可能出现 `.ipynb.ipynb`；临时输出、旧脚本和中间方案因为被当作"过程证据"长期占据 Current。
-   错在哪里：没有区分版本化候选、稳定 Current、历史 Archive 和临时过程产物，或重命名前没有核对实际扩展名。
-   正确做法：候选可以携带版本号和修复说明；正式 Current 使用稳定、无版本号的操作型名称和 URL；历史版本进入 Archive；有效知识进入权威文档，不能让所有临时输出、旧脚本和中间方案长期占据 Current。移动或重命名前核对实际文件名和目标扩展名；Notebook 内部版本保留在元数据或代码中；文件是否删除由用户逐项确认。
-   验收方式：目录中能一眼识别唯一 Current；同一功能不留下多个看起来像当前版本的入口；历史版本在 Archive 中按名称可排序；交接文档只指向正式入口；文件只有一个正确扩展名，内部版本可从 Notebook 自身读取。

### P023｜直接交付 XLSX，而可维护的 Colab 更合适

-   状态：已确认的交付决策问题。
-   场景：需要一次生成很多结构类似的表格文件，或未来还会反复调整字段和格式。
-   错误表现：逐个生成文件耗时，后续规则变化需要全部重做，用户无法自行复跑。
-   错在哪里：没有在产出前比较交付格式的生成速度、复用性和维护成本。
-   正确做法：产出前先判断并推荐最适合的形式。一次性定稿数据可直接
    XLSX；重复生成、批量结构和未来可调参数通常优先完整 Colab；长文档优先
    Markdown，只有版式要求高时使用 DOCX/PDF。
-   验收方式：交付方式能覆盖当前需求，并说明后续修改入口；用户不需要靠重新发起同样任务才能复现。

### P024｜只给补丁，没有给完整 Notebook

-   状态：已确认的交付问题。
-   场景：用户要求"新的完整 Colab"，交付却只有需要手工复制的 Cell
    或代码片段。
-   错误表现：用户无法确定片段插入位置，旧代码残留，新旧版本混杂。
-   错在哪里：把开发差异当成最终用户产物。
-   正确做法：当用户明确要求完整 Notebook 时，交付可从头运行的完整
    `.ipynb`；补丁只用于解释，不替代最终文件。
-   验收方式：在干净环境打开 Notebook，自 Cell 1
    起可顺序运行；文件名、版本记录和 Controller 完整。

------------------------------------------------------------------------

## 7. Sheets 展示与 Operator-First 设计

### P025｜为了层级完整使用三层表头

-   状态：已确认的阅读偏好。
-   场景：Overview 同时展示业务块、Campaign
    和指标，设计成三层或更多表头。
-   错误表现：信息结构理论上完整，但用户不习惯，定位数据更慢。
-   错在哪里：用抽象层级替代实际阅读效率。
-   正确做法：保持两层表头；通过业务块颜色、Campaign
    交替色、边框和留白表达结构，不额外增加表头层级。
-   验收方式：用户能从任一数值快速回溯到对应行和指标；冻结区域后仍可清楚阅读。

### P026｜数据区完全无色，容易看错行

-   状态：已确认。
-   场景：宽表只给表头上色，数据区全部白底。
-   错误表现：横向查看多个指标时容易把 A6 看成 A7，尤其在长 Campaign
    表中。
-   错在哪里：把"简洁"理解为取消所有视觉定位辅助。
-   正确做法：字段列可保持中性；数据区按业务块使用非常浅的底色，并结合斑马线。相邻
    Campaign 至少有可感知差异，但不能用高饱和色干扰数值。
-   验收方式：在正常缩放和宽表滚动情况下，随机选取数值能够快速定位到正确行；打印或灰阶下仍有边框/斑马线辅助。

### P027｜不同 Campaign 使用完全相同的色块

-   状态：已确认。
-   场景：同一个业务块下的多个 Campaign 全部使用同一底色。
-   错误表现：Campaign 边界不明显，纵向扫描时容易串行。
-   错在哪里：只表达了业务块，没有表达同级实体之间的分隔。
-   正确做法：业务块保持统一色系，Campaign
    在该色系内做深浅交替或斑马线；不要给每个 Campaign 随机换高饱和色。
-   验收方式：无需反复查看名称列即可辨认 Campaign
    边界，同时整体仍能看出所属业务块。

------------------------------------------------------------------------

## 8. 网络与本地运行环境

### P028｜TUN 与系统代理双开导致超时或回环

-   状态：已确认的常见网络路径问题。
-   场景：Clash Verge 等工具同时开启 TUN
    和系统代理，或前后两层代理路由互相承接不清。
-   错误表现：开启虚拟网卡后所有请求
    timeout；关闭后立即恢复；重装软件通常无效。
-   错在哪里：流量可能被重复接管、发生代理回环，或
    DNS/路由同时被两套机制改写。
-   正确做法：先采用单一接管方式排查：开 TUN 时关闭系统代理；确认
    DNS、路由和 bypass
    后，再逐层验证出口代理。不要在路径尚未验证时同时叠加多种接管方式。
-   验收方式：依次测试直连、TUN 单开、出口层单独连通和最终出口
    IP；每增加一层都确认 DNS 与目标应用仍正常。

### P029｜只验证浏览器出口，没有验证 Codex/CLI 实际出口

-   状态：已确认的排查问题。
-   场景：浏览器能访问目标服务，但 Codex、CLI、Google Ads Editor
    或其他进程无法连接。
-   错误表现：误以为整个系统都走同一代理，实际不同进程使用不同网络栈或绕过系统代理。
-   错在哪里：用一个应用的出口代表所有应用。
-   正确做法：对目标进程本身检查进程、环境变量、代理继承和实际出口；分别测试
    HTTP、HTTPS、DNS 与目标域名。TUN
    方案应以目标应用的实际连接为验收对象。
-   验收方式：从目标应用或其同一运行环境查询出口
    IP，并与预期代理出口比较；浏览器结果只能作为辅助。

------------------------------------------------------------------------

## 9. 新条目模板

复制以下结构追加新问题：

``` markdown
### PXXX｜简短问题名称

- 状态：已确认 / 待验证 / 已废弃。
- 场景：在什么系统、什么步骤、什么输入下发生。
- 错误表现：关键错误码、错误文本或业务异常。
- 错在哪里：根因，或明确标注当前只是推测。
- 正确做法：以后应采用的处理方式。
- 验收方式：如何证明修复真正有效，而不只是代码不再报错。
```

------------------------------------------------------------------------

## 10. 文档与知识资产管理

### P030｜Markdown 长表格不适合作为工具资产索引

-   状态：已确认。
-   场景：整理 Photoshop JSX、脚本、工具等个人资产索引时，尝试使用
    Markdown 表格记录文件名、用途、参数和说明。
-   错误表现：长文件名、长参数列表导致表格换行混乱；在 Obsidian
    中阅读困难，横向比较反而降低查找效率。
-   错在哪里：把"结构化"简单等同于"表格"。Markdown
    表格适合短字段比较，不适合承载长文本说明、代码参数和工具使用场景。
-   正确做法：工具类资产优先使用信息卡结构：
    -   标题（文件名）
    -   用途
    -   输入
    -   输出
    -   什么时候用
    -   可配置参数
    -   主要修改位置
-   适用范围：
    -   Script Index
    -   Tool Registry
    -   自动化工具说明
    -   个人知识资产目录
-   不适用：
    -   少量字段横向比较
    -   数据分析结果表
    -   需要批量筛选的数据清单
-   验收方式：打开 Obsidian 检查长内容阅读体验；确认 AI
    能通过固定结构快速理解工具职责。

### P031｜文件名应表达能力，不应误导默认参数

-   状态：已确认。
-   场景：整理 Photoshop JSX
    工具名称时，发现部分文件名包含固定尺寸，例如
    `140x140`，但代码内部尺寸参数实际可调整。
-   错误表现：文件名让使用者误认为工具只能生成某个固定尺寸。
-   错在哪里：把当前默认配置值当成工具能力。
-   正确做法：
    -   固定能力才写入文件名，例如 `JPG_Max2000px`。
    -   可调整参数使用 `SizePx`、`Configurable` 等表达。
-   验收方式：仅看文件名即可判断工具用途，但不会误解其限制。


# Authentication 与 Secret 管理

## P032｜OAuth Client Secret 生命周期误判

状态：

已确认。

场景：

创建 Google OAuth Client 后，需要保存 client_secret 用于无弹窗认证。

错误表现：

后续无法再次查看完整 secret，误认为凭据丢失。

错误原因：

OAuth Client Secret 通常只在创建阶段完整展示。

正确做法：

创建后立即保存：

-   Client ID
-   Client Secret
-   JSON 文件

如果遗失：

-   创建新的凭据；
-   不覆盖旧配置；
-   新配置验证成功后再切换。

验收：

确认：

-   credentials 可以生成；
-   refresh token 可以工作；
-   API 可以访问。

------------------------------------------------------------------------

## P033｜OAuth JSON 字段含义混淆

状态：

已确认。

场景：

读取 OAuth JSON 时，将 token_uri 与 refresh_token 混淆。

错误表现：

无法建立无弹窗认证。

错误原因：

token_uri：

是 Google Token Endpoint 地址。

refresh_token：

是授权后的长期刷新凭据。

两者用途不同。

正确做法：

明确保存：

    CLIENT_ID
    CLIENT_SECRET
    REFRESH_TOKEN

使用 OAuth Library 管理流程。

验收：

credentials 可以刷新 access token，并成功调用 API。

------------------------------------------------------------------------

## P034｜双 Runtime 不应维护两套业务代码

状态：

已确认。

场景：

同一个自动化任务需要：

-   Colab 运行；
-   VSCode 本地运行。

错误表现：

维护两份逻辑相同但环境不同的代码。

错误原因：

把运行环境差异混入业务逻辑。

正确做法：

保持：

    Business Logic

单一。

通过：

-   Secret Loader
-   Runtime Adapter
-   Environment Config

处理环境差异。

验收：

同一个 Notebook：

-   Colab 可运行；
-   Local Kernel 可运行；
-   输出结果一致。

------------------------------------------------------------------------

## P035｜混淆 Local 依赖安装与 VSCode 环境注入生命周期

状态：

已确认。

场景：

同一个 Notebook 需要在 Local VSCode 中使用专用虚拟环境和本地 Secret。

错误表现：

-   Local Kernel 缺少运行依赖；
-   从桌面或 Finder 启动 VSCode 后读不到终端中的环境变量；
-   VSCode 界面中选了看似正确的 Python Interpreter，但 Notebook 实际使用的 Jupyter Kernel、metadata 或进程环境并不一致；
-   误以为每次运行前都需要重新安装依赖。

错误原因：

把三个不同生命周期混为一体：

-   虚拟环境创建；
-   依赖安装；
-   每个 VSCode 进程的环境变量注入。

同时混淆四个不同层次：

-   Python Interpreter；
-   Jupyter Kernel / kernelspec；
-   Notebook metadata 中记录的 Kernel 选择；
-   VSCode 或 Jupyter 进程实际继承的环境变量。

正确做法：

-   为项目建立专用虚拟环境；
-   确认真正被选中的 Jupyter kernelspec，并让它指向同一个虚拟环境；
-   依赖通常每个虚拟环境安装一次；
-   Local Notebook 缺依赖时快速失败并打印精确安装命令；
-   需要本地环境变量时，通过本地启动器启动新的 VSCode 进程；
-   排查时分别核对 Interpreter 路径、kernelspec resource directory、Notebook metadata 和进程环境，不用 UI 中的 Interpreter 选择替代 Kernel 验证；
-   启动器和 Secret 文件保留在不参与云同步的本地目录。

验收：

-   当前 Kernel 的 Python 路径属于目标虚拟环境；
-   kernelspec、Notebook metadata 和当前 Kernel 的 Python executable 指向同一套预期环境；
-   依赖预检通过；
-   新 VSCode 进程能读取所需 Secret 名称；
-   Notebook 不在 Local Runtime 中自动安装或修改依赖。

------------------------------------------------------------------------

## P036｜静态检查通过后未做 clean-kernel Run All

状态：

已确认。

场景：

Notebook 已通过语法或静态编译检查，但正式运行时出现未定义名称或遗漏导入。

错误表现：

运行到实际初始化路径时出现 `NameError`，例如使用 `defaultdict` 但没有对应导入。

错误原因：

静态检查只能证明部分语法成立，不能证明：

-   所有运行路径所需名称已导入；
-   Cell 顺序正确；
-   当前 Kernel 没有依赖历史状态；
-   从零开始可以完整执行。

正确做法：

正式候选执行：

    Restart Kernel
        ↓
    Run All

不要用旧 Kernel 中残留的变量掩盖缺失导入或 Cell 顺序问题。

验收：

-   干净 Kernel 从 Cell 1 开始执行；
-   所有 Cell 按顺序完成；
-   没有依赖手工预先运行的隐藏状态；
-   再依据任务风险完成 Dry Run 或 live acceptance。

------------------------------------------------------------------------

## P037｜把单次远程大文件读取异常判定为正式数据损坏

状态：

已确认。

场景：

验证任务从远程 Drive 下载并检查大量 Parquet 文件。

错误表现：

一次运行出现 Raw / Curated 异常或文件读取不完整，立即推断正式数据已经损坏。

错误原因：

只依据单次传输或单次读取结果作结论，没有区分：

-   远程传输的暂时性中断；
-   不完整下载或缓存状态；
-   可重复的数据一致性问题。

正确做法：

-   使用分块下载；
-   对暂时性网络错误执行有限重试；
-   校验文件大小和 Parquet 可打开性；
-   重新运行同一范围；
-   必要时使用另一 Runtime 复核关键计数；
-   权限、Schema、配置和确定性数据错误仍应快速失败，不能无限重试。

验收：

只有在可重复读取后，才能把一致性差异归因于正式数据。若重试后 Local 与 Colab 的文件数、行数、Primary Key 和 Raw / Curated reconciliation 一致，则不得继续宣称数据损坏。

------------------------------------------------------------------------

## P038｜多 Dataset 验证时 last_run_id 未对齐

状态：

已确认。

场景：

验证作业计划同时验证多个 Dataset，但此前只单独重跑其中一个 Dataset。

错误表现：

各 Current Dataset 的 `last_run_id` 不一致，验证无法选择唯一的正式同步运行。

错误原因：

跨 Dataset 验证需要一个可共同追溯的父运行；单 Dataset 重跑会改变局部状态，但不能自动代表其他 Dataset 属于同一次同步。

正确做法：

-   需要跨 Dataset 冻结结论时，在同一个父运行中同步全部计划 Dataset；
-   只验证单个 Dataset 时，显式缩小验证范围；
-   不得随意选择任意 `last_run_id` 来掩盖状态不一致；
-   验证计划应在读取文件前检查 Dataset 状态是否对齐并快速失败。

验收：

-   所有计划 Dataset 指向同一个可核对的父运行；或
-   验证范围明确限制为状态对齐的 Dataset；
-   Parent / Child Logs、写入分区和 Dataset Watermark 可以共同回溯。

------------------------------------------------------------------------

## P039｜关键文档压缩造成语义丢失

-   状态：已确认。
-   问题与适用范围：重写或大幅压缩 Handoff、Master Plan、Document Map、Handoff Index 等关键项目文档，或在跨阶段、跨窗口、Source Mode 切换时传递知识候选，仍有效的运行入口、资源边界、风险、故障经验、验收规则、停止条件或待处置知识可能被删除；适用于所有承担运行、排障、验收、交接或知识吸收职责的载体。
-   可观察表现：Diff 显示大量删除或整体重写，新文档只保留最新状态；格式检查通过，但旧版中的有效事实在新版无对应位置；前一阶段已判定需要吸收的知识候选，在后一阶段因来源上下文不可读而被错误判为无需处理。
-   根因：只按重复率或篇幅压缩，只保留最新状态，没有比较新旧版本的语义，并默认旧内容都已过时；或把 Source Mode 当作候选事实的唯一来源，没有把尚未处置的知识摘要作为跨阶段状态持续传递。
-   错误处理：因为新版更短就直接提交；只运行 Markdown 格式检查或只看 diff stat；看到大量删除却不逐项核对事实；切换窗口或 Source Mode 后丢弃此前已识别的候选，并以当前阶段无法重读原始上下文为由改判无需吸收。
-   正确处理：提交前执行只读语义保全审查，将删除内容分类为 `SUPERSEDED_CORRECTLY`、`DUPLICATE_REMOVED`、`HISTORICAL_ONLY`、`MOVED_TO_CORRECT_DOCUMENT`、`VALID_CONTENT_LOST`、`NEEDS_CONFIRMATION` 或 `OUT_OF_SCOPE_CHANGE`。存在 `VALID_CONTENT_LOST` 或 `OUT_OF_SCOPE_CHANGE` 时不得提交，只定向恢复有效内容，不机械恢复整份旧文档。已识别但尚未处置的 PKB 候选必须以 `PKB_SOURCE_SUMMARY` 持续携带，直至明确进入 `ABSORBED_INTO_PKB`、`ALREADY_COVERED_IN_PKB`、`USER_DECLINED` 或 `INVALIDATED_WITH_REASON` 之一。
-   验证方式：比较 HEAD 与工作树版本，逐项核对运行、配置、风险、故障、验收和停止条件，再运行 `git diff --check`；跨阶段流程还应逐项核对 `PKB_SOURCE_SUMMARY` 中的候选及其最终处置状态，确保没有无理由消失或改判。
-   防复发规则：关键文档出现高删除比例、整体重写、职责变化、大量迁移，运行、排障、验收、停止条件被压缩，或知识吸收流程切换阶段、窗口、Source Mode 时，语义保全审查是提交或结束流程的门禁。

## P040｜把已经完成的状态误判成前置条件失败

-   状态：已确认。
-   问题与适用范围：自动化只接受“尚未执行”的理想前置状态，遇到目标操作已经完成时错误停止；适用于 Git、文件迁移、发布、资源创建及其他应具备幂等性的流程。
-   可观察表现：流程要求位于功能分支，但实际已在主分支且主分支与目标分支指向同一提交；工具仍报告前置条件失败，并跳过可继续执行的只读验证。
-   根因：流程只定义成功和失败，没有定义“操作前已经处于目标状态”。
-   错误处理：机械执行重复操作，或因前置状态不符合脚本预期就判失败并停止验证。
-   正确处理：结果必须区分 `ACTION_COMPLETED_NOW`、`ALREADY_IN_DESIRED_STATE` 和 `BLOCKED`。合并场景中，若主分支与目标分支已指向同一提交，判定 `ALREADY_FAST_FORWARDED`，不重复合并但继续验证；若无法 fast-forward，则判定 `BLOCKED`，不自动创建 merge commit、rebase 或 squash。流程、Prompt 或 Skill 更新后，不自动重跑已经完成且已提交的历史工作；只有当重跑会改变真实结论、暴露遗漏或降低现存风险时才重新执行。
-   验证方式：比较分支引用和 merge-base，检查 HEAD 与工作树，并判断目标结果是否已经实际达到。
-   防复发规则：所有可重复运行的提示词和自动化都必须把“已达成”设计为独立成功状态；状态识别与结果验证不得绑定为同一个开关。工具改进只在下一次真实任务中自然验证，除非它确实改变决策、产出或风险。

## P041｜混淆 Current 身份与验收证据成熟度

-   状态：已确认。
-   问题与适用范围：正式资产的 Current 身份、稳定路径是否落位、发布位置、Evidence Maturity、用户验收和 Git 冻结是相互关联但不能互相替代的判断；适用于代码、Notebook、数据管道、正式文档、发布资产和外部交付物。
-   可观察表现：新版缺少同等级运行证据就被自动降级；文件一旦标记 Current 就被宣称已验收或冻结；旧版证据被用来证明当前版本；存在成功 Live Run 和用户确认，却无法证明运行对应哪个字节版本；准备先运行线上验收，之后才确定正式发布或 Runner 的唯一位置；Git 合并或冻结已经完成且工作树干净，但权威项目文档仍保留旧分支、旧 revision、旧 merge 状态或已经失效的 Next Action。
-   根因：把正式入口治理、稳定路径、运行证据、用户判断、版本控制状态和文档状态压缩成一个状态，或让证据缺少对资产完整 Hash 与内部版本的绑定；误把工作树干净当成所有权威文档已同步最终状态的证明。
-   错误处理：因证据不足撤销已确认的 Current 身份；因 Current、稳定路径或干净工作树存在就宣称 Accepted/Frozen 或全部收口完成；让旧版 evidence 自动继承给新版；从临时上传、下载目录或未知副本运行，再事后补写正式发布位置；把无法绑定 exact Hash 的用户运行说明伪造成仓库 exact-hash evidence；合并后不检查各权威文档中的状态字段和 Next Action。
-   正确处理：Current 身份依次依据用户当前明确确认、项目 `PROJECT.md`、`current/` 稳定路径、已验收的 Git 历史、Current Handoff 和 Index；稳定路径落位单独记录。双 Runtime、发布包或共享 Runner 流程应先稳定并提交本地 Current，再复制或覆盖到唯一正式运行位置，从该位置运行并保留证据，即 `Current → Publish → Run → Evidence`。验收成熟度另按静态检查、测试、Dry Run、Live Run、回读、reconciliation 和 Warning 解释判断，用户验收、Git 冻结和文档状态也分别记录。新版是 Current 但缺少同等级证据时标记 `EVIDENCE_GAP`；旧版 evidence 必须明确绑定旧版。正式运行 manifest 应记录资产内部版本和完整 SHA-256；无法绑定时只保持 `USER_PROVIDED` 或 `USER_CONFIRMED` 等实际证据等级。Git 合并或冻结后，对 PROJECT、Master Plan、Project Handoff、Module Handoff、Document Map 和 Handoff Index 执行定向状态一致性检查，更新仍有效的 branch、revision、merge 状态和 Next Action；工作树干净不能替代该检查。
-   验证方式：分别列出 Current 身份、稳定路径、正式运行位置、证据成熟度、用户验收、Git 状态和文档状态；确认每项 evidence 可追溯到对应资产。声称 exact-hash evidence 时，manifest 中的完整 SHA-256 和内部版本必须与被验资产一致。合并或冻结后逐份核对适用的权威文档，确认其中的分支、revision、merge 状态和 Next Action 与最终状态一致，且文档之间不存在冲突。
-   防复发规则：状态模型必须分设正式身份、路径落位、发布或运行位置、Evidence Maturity、用户验收、Git 冻结和文档状态字段；任何升级、降级、验收或收口结论都逐项核对，不能相互代替。工作树干净只作为 Git 检查结果，不作为多文档状态一致性的替代证据。

## P042｜刷新既有 OAuth Token 时擅自扩大授权范围

-   状态：已确认。
-   问题与适用范围：使用 Refresh Token 进行 Google OAuth 非交互式刷新时，刷新请求重新发送不同或扩大的 scopes；适用于复用既有离线授权的 Notebook、脚本和服务。
-   可观察表现：凭据存在但刷新报 `RefreshError invalid_scope`；短重试仍重复失败。
-   根因：Refresh Token 绑定原授权范围，刷新逻辑却把当前代码期望的更大 scopes 当成可在刷新阶段追加的参数。
-   错误处理：持续短重试，或在未判断根因时重新引入交互授权。
-   正确处理：按原授权范围刷新，刷新阶段不擅自增加 scopes；确需扩大权限时，把重新授权作为独立、明确的用户授权流程。
-   验证方式：使用原授权范围成功刷新 access token 并调用范围内 API；扩大权限的需求不会被刷新逻辑静默处理。
-   防复发规则：离线刷新逻辑不得自行改变或扩大 Refresh Token 的授权范围。

## P043｜Local Secret 自动发现遗漏真实约定目录

-   状态：已确认。
-   问题与适用范围：Local Notebook 或脚本依赖本地 Secret，但自动发现逻辑没有覆盖实际约定目录。
-   可观察表现：代码报告 `client_id`、`client_secret`、`refresh_token` 全部缺失，实际 Secret 文件或变量已经存在。
-   根因：把有限的默认搜索路径当成完整事实，没有支持显式目录、环境变量覆盖或受控路径发现。
-   错误处理：直接判定 VS Code、Python Kernel 或 OAuth 凭据失效；在日志中输出 Secret 值以排查。
-   正确处理：依次区分 Kernel 环境、路径发现、Secret 名称和 Token 刷新；支持显式 Secret 目录、环境变量覆盖和受控的约定路径发现。可以输出已检查目录和缺失名称，但不得输出 Secret 值。
-   验证方式：在显式目录、环境变量覆盖和约定目录三种入口分别完成加载测试；日志只包含路径和 Secret 名称，不包含值。
-   防复发规则：认证失败诊断必须把运行环境、路径发现、名称映射和 Token 刷新拆开检查。

## P044｜Sheets 逐区块读取与短重试放大 429 配额错误

-   状态：已确认。
-   问题与适用范围：审计 Google Sheets 宽表或重复区块时，对每个区块单独读取，并对配额错误进行连续短重试。
-   可观察表现：请求按 1、2、4 秒退避后仍持续返回 429，任务长时间撞击每分钟读取配额。
-   根因：API 请求数量过多，且重试策略没有识别配额恢复所需的完整窗口。
-   错误处理：保持逐区块读取并反复短重试，或把每次 429 当作独立瞬时网络错误。
-   正确处理：优先使用 `batchGet` 合并读取，在本地切片和校验；识别为配额错误时等待完整配额窗口，并限制重试次数。
-   验证方式：记录同一审计范围的请求数量，确认宽表读取由多次逐区块请求合并为批量请求；配额模拟测试不会在窗口内持续发请求。
-   防复发规则：宽表布局检查不得默认逐区块读取，配额错误不得只采用秒级短重试。

## P045｜重复区块的固定语义只写入首个区块

-   状态：已确认。
-   问题与适用范围：在横向或纵向重复的报表区块中生成 Sum、Compare 等固定标签或其他固定语义。
-   可观察表现：首个区块标签完整，后续区块缺失；数值可能存在，但读者无法确认行列语义。
-   根因：固定语义只按全表首列或首个区块写入，没有把它作为区块模板的一部分逐块生成。
-   错误处理：只修首个可见缺口，或只校验数值而不校验固定标签。
-   正确处理：为每个重复区块生成并写入固定标签，把标签位置和值纳入 reconciliation。
-   验证方式：遍历首个、中间和最后一个区块，确认固定标签完整且与区块定义一致；故意删除一个标签时 reconciliation 必须失败。
-   防复发规则：重复区块的固定语义必须按区块生成、写入和验证。

## P046｜分隔列只修复可见区域或单个 Tab

-   状态：已确认。
-   问题与适用范围：Google Sheets 多 Tab 宽表使用分隔列区分重复业务区块。
-   可观察表现：顶部区域已变为预期背景，中部仍继承 banding 或条件格式；不同 Tab 的分隔列宽度不一致。
-   根因：把分隔列当成局部单元格修饰，只格式化部分行或单个 Tab，没有建立跨 Tab 的整列组件规则。
-   错误处理：逐个修补当前可见范围，未检查整列、条件格式覆盖和其他 Tab。
-   正确处理：把分隔列作为跨 Tab、整列级视觉组件，集中生成背景、条件格式优先级和统一宽度规则。
-   验证方式：在每个适用 Tab 检查顶部、中部和底部单元格，以及列宽和条件格式；所有分隔列应呈现一致。
-   防复发规则：分隔列规则必须集中生成，并执行跨 Tab、整列范围验证。

## P047｜把未跟踪文件直接当作垃圾删除

-   状态：已确认。
-   问题与适用范围：项目收口或 Current 整理时发现未跟踪文件、已说明的本轮修改或来源不明的工作树修改，需要判断其是候选、重复资产、历史版本、收口来源、未知风险还是可删除临时文件。
-   可观察表现：仅凭 `git status` 中的 untracked 状态就计划删除，尚未确认它是否承载唯一内容、Current 身份或应归档历史；或把所有 dirty worktree 都视为同一种异常，导致已说明修改也被阻断。
-   根因：把版本控制状态误当成内容价值和资产身份，没有比较完整 Hash、内容语义、Current 依据和归档目标；没有区分已由当前任务说明的修改和来源不明的修改。
-   错误处理：直接批量删除未跟踪文件；只比较文件名或短 Hash；看到近似副本就假定内容相同；为了取得 clean 状态擅自 stash、restore、reset 或覆盖；把已说明修改当作未知风险停止。
-   正确处理：删除前逐项比较完整 Hash、内容语义、Current 身份和预期归档目标，展示 diff、定向变更计划和精确删除范围；已说明修改可以作为收口来源继续只读审计；未知修改才触发停止或 Dirty Worktree Triage；无法确认身份时停止并请求用户判断。
-   验证方式：删除计划能够为每个目标给出完整 Hash、与保留资产的语义关系、Current/Archive 归属和明确去向；未确认项不执行删除；工作树审计能标出每项修改是已说明来源还是未知来源。
-   防复发规则：未跟踪只表示未纳入 Git，不表示可删除；任何清理都必须先完成资产身份审计；不得为了 clean 状态绕过用户确认或语义保全。

## P048｜Notebook 输出和 metadata 改变文件身份并污染 Git diff

-   状态：已确认。
-   问题与适用范围：`.ipynb` 的输出、execution count、metadata、Kernel 信息和保存动作都会改变文件字节，并可能把未计划修改的 Notebook 标记为 modified；适用于 Jupyter Notebook、Colab、可执行文档、Git 提交和需要 exact-current evidence 的数据流程。
-   可观察表现：业务代码没有变化，但运行、清除输出、切换 Kernel 或保存 Notebook 后，完整文件 Hash 与提交时不同；Git diff 主要出现运行输出、执行计数或环境 metadata，随后难以判断运行证据是否对应当前字节，也容易把噪音与正式源码改动一起提交。
-   根因：把 Notebook 当成纯源码文件，忽略输出、执行计数和 metadata 也是 `.ipynb` JSON 的一部分。
-   错误处理：把 Hash 不一致直接判定为代码损坏，反复重跑或修改源码；反过来用运行后的 Hash 证明提交前的 Current；或未检查 diff 就把输出和 metadata 噪音与正式代码一起提交。
-   正确处理：在使用完整字节身份绑定前，先规定 Notebook 输出和 metadata 的处理方式。Current 编辑源应与运行输出分离；必要时使用执行副本、清除临时输出后再提交，或在 manifest 中明确记录运行对应的实际 Notebook 字节身份。提交前逐个核对 modified Notebook，只提交授权的源码变化；查看或运行后不需要保留输出时，关闭前不要保存。历史证据只能证明它绑定的字节，不能自动升级为当前文件证据。
-   验证方式：比较运行前后完整文件 Hash，并核对代码单元内容、Notebook metadata、Kernel 信息和证据 manifest 中记录的身份是否一致；提交前检查 Git 状态、diff stat 和目标文件 diff，确认变更文件及内容与授权范围一致。
-   防复发规则：Notebook 项目提交前必须确认每个 modified 文件是否包含真实源码变化；凡是声称 Notebook exact-current evidence 的流程，都必须先定义输出、execution count、metadata 和保存动作的处理规则，否则只能标记为较弱证据或 `EVIDENCE_GAP`。

## P049｜稳定治理规则和动态项目状态混入长期 Prompt 或 Skill

-   状态：已确认。
-   问题与适用范围：长期重复使用的 Prompt、项目 Skill、收口模板、交接流程或自动化任务同时承载稳定治理规则和当前窗口动态事实。
-   可观察表现：Prompt 或 Skill 过长；阶段变化后仍携带旧数字、分支、当前代码或下一步；旧状态可能被重新写回 Current。
-   根因：没有区分长期稳定的治理机制与当前窗口的动态事实。
-   错误处理：将当前进度、数量、分支、commit、当前代码和下一步永久写进重复使用的 Skill 或固定模板。
-   正确处理：Bridge Prompt 提取当前动态变化并生成一次性 Source Package；项目 Skill 只保存长期稳定的治理和文件职责。
-   验证方式：检查永久 Skill 是否不包含会随阶段变化的项目状态；动态事实是否来自当前 Source Package 和仓库验证。
-   防复发规则：会随阶段变化的事实不得写死在永久 Skill 中。
-   适用范围：项目 Skill、Bridge Prompt、收口模板、交接流程和自动化任务。

## P050｜范围配置未贯穿实际读取和处理层

-   状态：已确认。
-   问题与适用范围：任务提供日期、分区、对象或其他范围配置，但该配置只影响计划变量或部分检查，没有约束后续索引、读取和处理；适用于按范围运行的数据验证、同步、构建和批处理任务。
-   可观察表现：界面或配置显示已缩小范围，实际运行仍扫描或读取范围外的大量历史数据，耗时和资源消耗没有随范围缩小而下降。
-   根因：范围只传递到 expected scope 等计划层变量，没有作为同一约束继续传入索引筛选、文件读取和下游处理层。
-   错误处理：继续执行全量扫描；只修改计划摘要而不改变实际读取；或为加速而关闭核心验证。
-   正确处理：让同一范围配置同时约束预期范围、索引候选、实际读取和下游处理。范围外数据可以作为告警或审计证据，但不进入正式处理集合，除非任务明确要求全量审计。
-   验证方式：对照配置范围检查预期对象、范围内索引、实际读取对象和处理统计，确认各层边界一致；缩小范围后，读取量与处理量应相应下降。
-   防复发规则：任何范围配置都必须端到端传递到实际读取和处理层，不能只停留在计划、展示或预检查层。
-   适用范围：按日期、分区、对象或其他边界运行的数据验证、同步、构建和批处理任务。

## P051｜规则未确认前编写生成代码固化 Working Assumption

-   状态：已确认。
-   问题与适用范围：内容生成器、数据处理脚本、Notebook、Colab 和自动化流程在规则、映射、输入输出和 QA 未确认前开始实现。
-   可观察表现：生成脚本或 Notebook 编写后，规则、字段或验收条件发生变化，导致代码、输出和文档一起返工。
-   根因：执行顺序倒置，为尽快看到结果而先实现、后确认规则。
-   错误处理：在事实来源、字段职责、匹配逻辑、输出边界和 QA 标准尚未确定时直接编写生成器。
-   正确处理：先确认权威输入、规则、映射、输入输出和验收标准，再开始编写代码。
-   验证方式：代码开始前检查是否已有明确的规则状态、输入输出契约和 QA 门禁。
-   防复发规则：未满足实现开始条件时，只做规则和设计工作，不编写正式生成器。
-   适用范围：内容生成器、数据处理脚本、Notebook、Colab 和自动化流程。
