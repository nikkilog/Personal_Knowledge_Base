# 个人与工作上下文

> 用途：让新的 AI、协作者或未来的自己，在不重读历史聊天的情况下，快速理解“我是谁、我长期在做什么、我通常希望怎样工作”。
>
> 不记录：临时项目状态、版本、分支、commit、run_id、资源 ID、一次性报错或具体下一步。这些内容应留在对应项目仓库。
>
> 状态：Current
> 最后更新：2026-07-27

## 1. 我是谁

我是一个以独立站电商为核心的经营者、运营决策者和系统建设者。

我的工作不只限于营销或代码。我会同时处理商品、定价、广告、数据、网站结构、内容、联盟渠道和内部流程，并持续把零散操作沉淀为可以重复运行、验证和交接的系统。

我不是以专业软件工程师身份开展这些工作，但会深度参与业务逻辑、数据结构和自动化设计。我需要 AI 把业务语言翻译成可靠的代码和工具，同时保留我对最终逻辑、字段、流程和展示方式的控制权。

## 2. 长期业务与工作范围

这里只记录持续性的业务领域和长期工作流，不记录具体阶段、版本或下一步。

### Plumbingsell / PBS

- 站点：plumbingsell.com。
- 业务范围：管道、阀门、管件、管材、排水及相关产品，包括 PEX、PVC、CPVC、Copper、DWV 等。
- 长期工作范围：Shopify 商品与元字段、定价、Google Ads 数据基础与分析、商品关联购买、分类、页面结构、SEO、Feed 和联盟渠道治理。

### NRP

- 站点：nrpequip.com。
- 业务范围：Rubber Track 与 Undercarriage Parts。
- 长期工作范围：SKU 主数据、兼容品牌与机型、零件号、Feed、PDP Feature、产品分类和搜索匹配判定。

### Apollo

- 站点：apolloliftus.com。
- 业务范围：Pallet Jack、Stacker、Lift Table 等物料搬运设备。
- 长期工作范围：经营与广告分析、Google Ads 结构、类目表现和周期报告。

### Commerce Analytics Hub（CAH）

- 性质：跨数据源的电商分析与数据基础设施。
- 常用数据源：Shopify、GA4、Google Search Console、Google Ads、Merchant Center、Meta Ads、AWIN、Google Sheets。
- 核心理念：Operator-First。每张正式输出首先应是经营者能够直接阅读、判断和行动的视图，而不只是技术中间表。
- 常见载体：Google Sheets、Google Drive、Colab、Parquet、CSV/XLSX，以及必要的 Markdown 交接文档。

## 3. 长期目标

1. 把依赖记忆和手工操作的电商工作逐步变成结构清楚、可重复运行、可校验的系统。
2. 建立统一的数据基础，让商品、销售、广告、成本、定价和经营分析能够互相连接。
3. 让系统适合当前实际操作并可以逐步扩展，而不是为了架构完整一次做得过重。
4. 降低 Colab、Google Sheets 和脚本的维护难度；出错时快速定位，不重新猜整段历史。
5. 把重要方法、决策和项目现场沉淀为知识资产，使新对话、新 AI 或新协作者可以直接接手。
6. 建立适用于多数主题的个人知识管理方式：临时思考可以自由发生，成熟内容再进入长期资料。

## 4. 思考与决策习惯

- 我通常从实际操作顺序出发，再决定系统结构。
- 我重视逻辑是否容易理解、维护和修改，不追求不必要的工程复杂度。
- 优先采用能够直接完成目标的最简单路径。增加步骤前，先判断它是否会改变决策、产生必要输出或降低真实风险；如果不会，不应因为流程惯性而增加重复审计、前置检查或人工校验。
- 低风险本地迁移已有明确 Current 和可回退版本时，优先提供直接、可运行、可回退的验证步骤；只有发现实际歧义、历史取舍或破坏风险时，才升级为详细的 Git 历史与索引取证。
- 我更喜欢多个职责单一的 Notebook，而不是把所有流程塞进一本超大 Notebook。
- 我偏好两层信息结构，主要适用于经营报表、Dashboard、Google Sheets 和页面信息层级；不应机械套用到所有代码、技术文档或目录结构。
- 我对颜色和视觉可读性敏感。颜色应帮助区分区域、行或业务含义，不能只作装饰。
- 我会在讨论中逐步澄清真实需求。实现前应区分已确认规则、Working Assumption 和探索性想法。
- 长窗口或多轮设计进入正式编码前，应重新核对最初范围和历次确认，逐项建立 requirements-to-code 对照，不能只凭记忆实现。发现一处契约遗漏时，应同步检查配置、读取、处理、执行、结果回写、Runtime 加载和测试等相邻阶段是否仍保留旧逻辑；交付前再做遗漏审查，确认已讨论规则真实进入代码、调用顺序和贴近实际使用形态的测试，而不只确认主流程能够运行。
- 我接受 AI 做合理判断，但不希望 AI 在会改变业务含义的地方静默猜测。
- 资料不足且用户能够提供证据时，应直接列出需要的文件、命令输出、日志或截图，不要反复基于假设推进。
- 对耗时或成本较高的任务，小规模功能验收通过后可以先完成本轮收口，把全量运行留到后续；必须明确区分“小规模验证通过”与“全量完成”。
- 宣布多阶段任务完成前，必须重新对照最初范围、权威设计和后续确认，将事项区分为：已完成、已完成小规模验证、明确延期、明确取消和尚未实现。

## 5. ChatGPT、Codex 与项目收口协作

- 当前聊天拥有完整上下文时，优先由当前 ChatGPT 整理规则、Bridge Package、Source Package 或文档方案；Codex 负责本地读取、文件更新、验证和 Git 收口。
- Codex 的主要价值是维护本地项目的长期可接手性，而不只是编写或运行代码。
- 复杂思考、代码或 Notebook 构建、项目规划和规则审查开始前，用户要求参考 PKB 时，应先根据当前任务检索相关知识，只读取命中的文件和必要章节或段落；相关内容不足时再扩大范围，不为形式完整而顺序全文读取所有核心文件。用户可以用简短的同义指令触发，不要求使用唯一固定口令。
- 使用长期知识时应区分三层来源：PKB 负责长期偏好、治理原则和 Pitfall；当前项目仓库、正式模块和用户提供的参考文件负责当前实现事实；当前聊天负责本轮目标、参数、Working Assumption 和操作授权。三层不能互相替代。
- 涉及现有项目时，已有 Current、真实调用链和已运行参考实现优先于通用经验。不得仅凭 PKB 的方向性规则自行发明 Secret 名称、认证方式、字段契约、模块路径、Runtime Adapter 或平行实现；项目证据缺失时，应明确说明缺少什么并请求资料，而不是继续补猜。
- 用户明确要求先检索 PKB 时，正式构建前应提供简短检索回执：实际命中的 PKB 文件或段落、实际读取的项目权威来源、准备沿用的既有契约，以及仍未取得证据的事项。
- 项目文档应让后续人员或 AI 不依赖聊天历史就能理解：项目目的、设计逻辑、当前规则、标准步骤、完成状态、唯一 Current、已废弃方向、历史 Pitfall 和唯一下一步。
- 稳定、可复用的治理规则应沉淀到 Skill；聊天窗口使用较短的 Bridge Prompt 提取当前动态事实和验收边界。阶段、数量、分支、commit、Current 代码和下一步不得永久写死在 Skill 中。
- Skill 应先形成真实任务可用的最小版本，再根据实际运行结果迭代，不必首次覆盖所有边缘情况。
- 项目收口后的 PKB 知识吸收应主动进行，但个人长期偏好不等同于本次删除、提交、发布或跨仓库操作授权；操作门禁由 PKB 的 `AGENTS.md` 管理。

## 6. 沟通与交付偏好

- 先说结论，再解释必要原因。
- 使用清楚、直接的中文；技术术语只在有帮助时出现。
- 不需要大量客套、重复总结或为了显得完整而堆砌内容。
- 生成给用户使用的 Markdown 文件时，默认采用紧凑 Markdown：减少无意义空行和连续换行，标题、字段、信息卡及关联内容尽量紧凑；确需视觉分区时才增加空行。
- 复杂需求先整理成明确规则，再动代码。
- 关键歧义应说明它会影响什么；不影响主方向的小问题可以采用合理默认值并明确说明。
- 失败、数据不完整或只完成一部分时必须直说，不能用“已完成”掩盖问题。
- 运营报告默认先给数据，再给 4–6 条面向非技术阅读者的结论。
- 需要复制到 Google Sheets 或飞书的表格，默认使用 Tab 分隔文本，不使用 Markdown 表格。
- 当一种交付方式明显更快、更容易维护时，希望 AI 主动推荐，例如 Markdown、完整 Notebook、XLSX、Google Sheets、DOCX、PDF 或 Slides。

### “本步安排”默认内容

项目或代码任务需要正式说明“本步安排”时，默认包括：

1. 当前阶段进度；
2. 设计目的；
3. 可选方案及取舍；
4. 推进价值；
5. 执行者；
6. 推荐模型；
7. 预计操作时间；
8. Token 消耗；
9. 本步不需要做的事项。

不适用项应标记为不适用，不为凑齐格式增加无价值步骤。

## 7. 代码与 Notebook 偏好

### Controller、Runtime 与认证

- 面向多个同类项目建设工具时，默认目标是让项目通过规范化的项目代码、路由、表格配置、Secret 命名、字段字典和 Job 参数即插即拔接入；历史特例或临时路由不能反向定义框架的正式职责，也不应因切换项目而复制或重写核心业务代码。跨项目公共能力优先由 Workspace 级共享组件承载；每套独立代码只在 Secret Loader 或认证边界接入一次，不逐个修改 API 调用点，也不为每个新账号维护硬编码映射。
- Notebook 的 Cell 结构先按实现形态分类，不能把固定 Cell 数机械套到所有 Colab。当 Colab 主要作为外部可复用 Python 模块、GitHub `.py` 或本地包的薄入口时，默认采用 4 Cell：Cell 0 是默认关闭的可选 Notebook Registry；Cell 1 是统一 Controller，集中业务参数、运行开关、Secret 名称、阈值和输入输出位置；Cell 2 完成认证、依赖、代码刷新和模块加载；Cell 3 执行任务、持续反馈进度并输出最终结果。共享 Python 没有当前项目专用 Notebook 入口，不等于能力缺失，也不需要为形式完整维护近似副本。
- 当主要业务逻辑直接保存在独立 `.ipynb` 中时，不固定为 4 Cell。应按 Controller、Runtime/依赖、认证、核心处理、读取、写入、执行与摘要等真实功能自然拆分；每个 Cell 保持适中、职责清楚且可顺序运行，既不把数百行业务函数塞进一个超长 Cell，也不为了增加 Cell 数拆成缺乏独立意义的碎片。
- Notebook Registry 是辅助功能，默认关闭且不得默认阻断业务任务。启用后应按 `job_name + sheet_label + Tab name` 的逻辑唯一键定位记录：零条或多条匹配时明确 warning，不自动追加或任意修改；只有一条匹配时才定向更新。默认非 strict 模式继续主任务，只有操作者显式启用 strict 才允许登记异常中止运行。
- Registry 只按需同步容易变化的 Notebook URL 和名称；用途、排序、说明、审核状态、逻辑键和主要操作字段等低频治理信息由表中人工维护。主要操作字段只登记会改变 Job 行为且操作者需要知道允许值的控制字段，例如 `action`、`op`、`mode`、`review_status` 或 `write_mode`，不罗列普通输入列。
- 同一本 Notebook 应支持 Colab 与 Local Runtime，业务逻辑保持单一，通过 Secret Loader、Runtime Adapter 和环境配置处理差异。
- “正式免弹窗版”指使用已确认的 Runtime Adapter、Secret Loader 和项目专用 Secret 映射，使 Colab 与 Local Runtime 都能采用预配置的非交互认证；它不等同于简单改用 Service Account。
- 如果确实必须交互授权，应紧接 Cell 1 发生，不应运行到中途才弹窗。
- 创建文件、备份或权限确实需要 OAuth 时，应说明认证类型和交互要求。
- 主流程应自上而下运行，不依赖中途手工修改隐藏变量。

### 写入、失败与验收

- 正式全量替换场景默认使用明确的覆盖逻辑；不随意新建 Tab，同名目标应按已确认规则清空后写入或覆盖。
- Google 写入前根据风险使用 Dry Run 或 Audit；覆盖现有正式数据、结构变化、删除或不可逆操作应备份。可逆、可重跑、目标为空或已有充分版本历史时，不为了形式额外堆积备份。
- 已有资产修复优先 targeted repair；只有结构确实不可修复或用户明确授权时才 destructive rebuild。
- 关键写入后应做与风险相称的回读、行数、字段或汇总校验。
- 代码必须显式失败：关键输入缺失、读取为空、字段不符、写入不完整或回读不一致时，抛出清楚异常。
- 不接受静默跳过、吞掉异常继续运行或用空结果冒充成功。
- 业务规则应集中定义，避免同一个阈值或映射散落在多个 Cell。
- 能由配置和字段字典稳定描述的字段标识、数据类型、表达式、导出与聚合规则、模板、固定值、继承和清除规则，应优先由统一配置驱动，不在多个读取、编译和写入模块中重复硬编码。自动同步系统事实时只更新明确受管字段，不覆盖人工维护的用途、顺序、审核状态、业务分类和说明。
- 输入 Schema 变化必须端到端同步模板、字段常量、读取、校验、默认值、处理、输出和说明。
- 能由代码可靠维护的运行数据、状态、镜像、目标创建和容量扩充，应由代码自动完成；解释性资产由人工维护。
- 人工输入、机器契约、验证预览、人工审核、真实写入和结果证据应保持分层。大规模操作先完整读取与预检输入、批量读取当前事实、生成执行计划和便于人工核对的 Preview，再通过明确的真实写入门分批执行；不采用边读取边盲写。
- 中间表和 Prepare 结果是计划快照。Apply 前必须重新读取会影响真实写入的关键配置、目标对象状态和冲突条件，不能把准备阶段的事实天然视为执行时的最终事实。
- 高风险写入默认提供 Dry Run、额外确认和合理的对象或批次上限，只处理明确获批的对象。多步骤创建应先保持非公开或草稿状态，关键步骤全部成功后再切换目标状态；部分失败时保留安全状态并明确报告，避免半成品上线。
- 历史实现存在差异时，先区分当前真实行为、界面或注释声称的行为、计划能力、局部验证结果和历史遗留，再识别权威来源；不能仅因版本或结构不统一就自动重构。仍处于设计、试验或局部验证阶段的模块只能作为设计素材和已验证局部能力，不能直接视为正式标准。

### 运行摘要

可能持续较长时间的 Notebook、脚本、批处理、同步、导出或写入任务不能沉默运行。运行中应以阶段切换、每批或每 N 条更新、单行动态进度或周期性 heartbeat 提供适度反馈，包括当前阶段、已完成量/总量、当前对象或批次、成功/失败/跳过、warning/error、重试或限流等待、fallback/partial failure，以及在信息足够时给出合理 ETA 或剩余量判断；错误和重试应立即提示，但避免逐行刷屏。操作者在结束前就应能判断任务是否仍正常、处理到哪里和是否卡住。

运行结束应输出摘要，适用时包括：

- `runtime_mode`
- `auth_type`
- `interactive_auth_used`
- `run_id`
- `rows_loaded`
- `rows_pending`
- `rows_planned`
- `rows_written`
- `rows_skipped`
- `warning_count`
- `error_count`
- `business_objects_planned`
- `api_operations_planned`
- `api_operations_succeeded`
- `api_operations_failed`
- 输出视图列表

输入行、通过校验的行、计划业务对象、计划 API 操作、成功或失败的 API 操作和实际写入行必须按真实语义区分；多个输入行可能合并为一个操作，不能把输入行数直接当成写入次数。只有存在独立“识别”阶段时才额外使用 `rows_recognized`，不把它强制为所有 Notebook 的通用字段。

### 交付范围

- 只涉及 1–2 个 Cell 的局部修改时，优先直接提供可以完整替换的 Cell。
- 修改范围更大且用户已提供 Notebook 时，优先直接交付可从头运行的完整新文件，不因 Cell 数量机械升级到 Codex。
- 涉及本地仓库、跨文件、Git 提交、复杂验证、正式文档收口或影响范围不明确时，再升级为 Codex 任务。
- 用户明确要求完整 Notebook 时，补丁不能替代最终 `.ipynb` 文件。
- 代码注释、配置说明和报错解释优先使用中文；API 字段、库名、Sheet 名和专有名词保留原文。

## 8. 文件与版本管理偏好

- 仓库内的正式 Current 使用稳定、无版本号的操作型名称和固定位置，日常修改原位更新。
- 普通历史由 Git History 保存；只有重大修改、非 Git 资产或具有独立追溯价值的旧资产才进入 Archive。
- 聊天窗口交付修改后的完整文件时使用新的明确文件名，避免浏览器缓存、旧附件或同名下载混淆；用户确认后再放回仓库的稳定 Current 名称。
- 修改或交付仓库代码时，以真实仓库树、目录和模块名为权威，不得擅自重命名、迁移或改写 import 身份。下载交付件可以使用版本化名称避免缓存，但其内容必须能够按获批方式覆盖仓库中既有的正式文件，下载名不能反向改变 Current 的正式路径或模块名。
- Runtime 执行副本只服务本地执行或调度，不反向定义源码 Current；源码或 runner 更新后按规则刷新 Runtime copy。
- 远程下载、动态 import 或容易受缓存影响的 Python 模块是稳定 Current 命名规则的例外：交付时使用新版本化文件名，并在运行入口打印实际文件路径和版本。详细经验见 `PITFALL_LOG.md` 的 P054。
- 本地 Runtime 目录使用稳定层级结构，避免散落为临时平行目录；PKB 只记录组织原则，不保存项目绝对路径。
- 不使用 `final_final`、`最新版2` 等无法判断顺序的名称。
- Notebook 常用动词前缀：`Create_`、`Build_`、`Run_`；名称应直接说明用途。
- 项目专属 Skill、工具和执行入口应明确表达项目与用途，优先清楚、可搜索、可识别。
- 治理体系或 Prompt、Skill 升级时，默认同时交付可直接交给 Codex 的完整更新指令，以及该版本全部新 Prompt 和交付文件的可下载打包文件。
- Notebook 内部版本保留在元数据或代码中，不写进正式 Current 文件名。
- 同一主题尽量只有一个明确 Current；旧代码或旧产物退出 Current 前，应列出候选、用途、处置原因和需迁移知识，并由用户确认。
- 每个本地 Python 项目只使用一套明确的项目专用虚拟环境，并通过依赖清单和安装脚本重建；不提交或分发整个虚拟环境。
- Python 虚拟环境与 Secret 配置分开管理。Secret 文件集中存放在受控且不参与云同步的本地位置，共享 Resolver 只保存解析逻辑、非敏感命名合同和测试，不保存 Secret 值；程序通过规范化项目代码和文件命名自动路由到当前项目明确允许的配置，不扫描、猜测或混用其他项目配置。

## 9. 数据与输出原则

- 业务口径先于图表和代码；同名指标必须说明时间范围、粒度和计算方式。
- 唯一键、日期范围、去重规则和汇总粒度必须显式定义。
- 输出应能被经营者直接读懂，并能追溯到来源和运行配置。
- 对需要断点恢复或在线查看的长任务，本地文件先安全落盘并作为 Source of Truth；在线表格只作为代码维护的镜像，可以在本地批次成功后逐步同步。
- 默认只把正式业务输出同步到线上；运行日志、失败明细和诊断文件优先保留本地，除非用户明确要求同步。
- 近期广告数据可能重新归因；校验应区分真实错误与可解释的小幅变化，同时保留严格模式。
- 数据区格式和颜色用于防止错行、错列和误读；字段列与数值区可以采用不同视觉规则。
- 不因技术便利改变已确认的业务定义。

## 10. 常用技术环境

- Shopify / Shopify GraphQL
- Google Ads API
- GA4 / Search Console / Merchant Center
- Google Sheets / Google Drive / Google Colab
- Python、Pandas、Parquet、CSV、XLSX
- Obsidian / Markdown
- GitHub / VS Code（在适用项目中）

## 11. 更新规则

- 只有长期稳定、会影响未来判断的信息才进入本文。
- 新信息与旧信息冲突时，以用户最近一次明确表述为准，并原位更新旧条目。
- 相同偏好只保留一个主条目；需要关联 Pitfall 时使用简短引用，不复制完整事故说明。
- 每次重大更新修改“最后更新”日期；历史由 Git 保存，不在正文堆叠流水账。
- 已经明确沉淀的长期协作偏好应在后续任务中默认执行，不要求用户反复重新说明。
- 本文不构成破坏性操作或 Git 写入授权；仓库操作边界以 `AGENTS.md` 为准。
