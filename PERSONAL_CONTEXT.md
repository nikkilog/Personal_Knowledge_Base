# 个人与工作上下文

> 用途：让新的 AI、协作者或未来的自己，在不重读历史聊天的情况下，快速理解“我是谁、我在做什么、我希望怎样工作”。
>
> 状态：Current
> 最后更新：2026-07-23

## 1. 我是谁

我是一个以独立站电商为核心的经营者、运营决策者和系统建设者。

我的工作并不只限于“做营销”或“写代码”。我会同时处理商品、定价、广告、数据、网站结构、内容、联盟渠道和内部流程，并持续把零散操作沉淀成可以反复运行、可以验证、可以交接的系统。

我不是以专业软件工程师身份开展这些工作，但会深度参与业务逻辑、数据结构和自动化设计。我需要 AI 把业务语言翻译成可靠的代码和工具，同时保留我对最终逻辑、字段、流程和展示方式的控制权。

## 2. 当前主要业务与项目

### Plumbingsell / PBS

- 站点：plumbingsell.com。
- 业务：管道、阀门、管件、管材、排水及相关产品，包括 PEX、PVC、CPVC、Copper、DWV 等。
- 当前重点：
  - Shopify 商品创建、编辑、导出和元字段体系。
  - 调价系统与价格变更事实表。
  - Google Ads 数据同步、校验、Overview 和分析基座。
  - Shopify 同单关联购买排名。
  - 商品分类、页面结构、SEO、Feed 和联盟渠道治理。

### NRP

- 站点：nrpequip.com。
- 业务：Rubber Track 与 Undercarriage Parts。
- 当前重点：SKU 主数据、兼容品牌与机型、零件号、Feed、PDP Feature、产品分类及搜索匹配判定。

### Apollo

- 站点：apolloliftus.com。
- 业务：Pallet Jack、Stacker、Lift Table 等物料搬运设备。
- 当前重点：经营与广告分析、Google Ads 结构调整、类目表现和半月/月度报告。

### Commerce Analytics Hub（CAH）

- 性质：跨数据源的电商分析与数据基础设施。
- 常用数据源：Shopify、GA4、Google Search Console、Google Ads、Merchant Center、Meta Ads、AWIN、Google Sheets。
- 核心理念：Operator-First。每张输出表首先应当是经营者可以直接阅读、判断和行动的视图，而不只是技术中间表。
- 常见载体：Google Sheets、Google Drive、Colab、Parquet、CSV/XLSX，以及必要的 Markdown 交接文档。

## 3. 长期目标

以下目标是根据长期工作方向归纳的，不代表已经设定了具体截止日期：

1. 把依赖记忆和手工操作的电商工作，逐步变成结构清楚、可重复运行、可校验的系统。
2. 建立统一的数据基础，让商品、销售、广告、成本、定价和经营分析能够互相连接。
3. 让每个系统既适合当前实际操作，又能够逐渐扩展，而不是为了“架构漂亮”一次做得过重。
4. 降低 Colab、Google Sheets 和各种脚本的维护难度；出错时能够快速定位，而不是重新猜整段历史。
5. 把重要方法、决策和项目现场沉淀为知识资产，使新对话、新 AI 或新协作者可以直接接手。
6. 建立一套适用于多数主题的个人知识管理方式：临时思考可以自由发生，成熟内容再进入长期资料。

## 4. 我的思考与决策习惯

- 我通常先从实际操作顺序出发，再决定系统结构。
- 我重视逻辑是否能被看懂、维护和修改，不追求不必要的工程复杂度。
- 优先采用能够直接完成目标的最简单路径。安排任何步骤前，应先判断它是否会改变决策、产生必要输出或降低真实风险；如果不会，就不应因为流程惯性或我提到过某件事而默认增加重复审计、人工校验、前置检查、环境检查或其他机制。
- 我更喜欢多个职责单一的 Colab，而不是把所有流程塞进一个超大 Notebook。
- 我习惯两层信息结构；展示层级太多会降低阅读效率。
- 我对颜色和视觉可读性敏感。颜色必须帮助区分区域、行或业务含义，不能只为了装饰。
- 我会在讨论中逐步澄清真实需求。因此，实现前应先识别哪些是已确认规则、哪些只是探索性想法。
- 当一种交付方式明显更快、更容易维护时，希望 AI 主动说明并推荐，例如 Markdown、DOCX、XLSX、Google Sheets、Colab、PDF 或 Slides。
- 对过去网页窗口做项目收口时，应由网页窗口一次生成完整、可直接粘贴到 Codex 的 Skill 调用包，包含来源、任务范围、任务类型和验收要求，不要求我手工拼接调用参数。
- 项目收口使用 Codex 的重点，是维护项目长期可接手性，而不只是编写或运行代码；后续人员或 AI 应能不依赖聊天历史就理解项目目的、设计逻辑、当前规则、标准步骤、已完成事项、当前阶段、唯一 Current、已废弃方向、历史 Pitfall 和唯一下一步。Evidence 应服务于这些判断，而不是为了堆积材料本身。
- 稳定、可复用的项目治理规则应沉淀到 Skill；聊天窗口只使用较短的 Bridge Prompt 提取本次现场事实、边界和验收要求，并生成一次性 Source Package，避免每次重复粘贴完整规则造成冲突或漂移。当前阶段、数量、分支、commit、当前代码和下一步等动态事实不得永久写死在 Skill 中。
- 当当前 ChatGPT 已拥有完整上下文时，优先由当前聊天生成 Skill、Bridge Package 或文档方案，Codex 负责本地读取、文件更新、验证和 Git 收口，以减少重复推导和额外 Token 消耗。
- Codex Skill 应先形成可在真实任务中使用的最小可用版本，再根据实际运行结果持续迭代；第一次创建不必预先覆盖所有边缘情况。
- 我接受 AI 做合理判断，但不希望 AI 在会改变业务含义的地方静默猜测。
- 需要判断本地状态、权限、脚本、配置、日志或 Git 范围时，如果资料不足，应直接列出需要我提供的文件、命令输出和日志片段，不要在可由我补充证据的情况下反复基于假设推进。
- 对耗时较长或成本较高的任务，小规模功能验收通过后可以先完成本轮收口，把全量运行留到后续；不得为了形式完整强加不会改变当前决策的全量步骤，但文档和结论必须明确区分“小规模验证通过”与“全量完成”。
- 宣布多阶段项目完成前，必须重新对照最初范围、权威设计文档和用户后续确认，将事项明确分为：已完成、已完成小规模验证、明确延期、明确取消和尚未实现；核心代码可运行不能替代端到端范围核对。

## 5. 沟通偏好

- 先说结论，再解释必要原因。
- 使用清楚、直接的中文；技术术语只在有帮助时出现。
- 不需要大量客套、重复总结或为了显得完整而堆砌内容。
- 复杂需求先整理成明确规则，再动代码。
- 如果存在关键歧义，应指出它会影响什么；不影响主方向的小问题可采用合理默认值并明确说明。
- 如果结果失败、数据不完整或只完成了一部分，必须直说，不能用“已完成”掩盖问题。
- 报告先给数据，再给 4–6 条面向非技术阅读者的结论。
- 需要复制进 Google Sheets 或飞书的表格，默认用 Tab 分隔文本，不使用 Markdown 表格。
- 给出项目或代码任务的“本步安排”时，默认先说明：当前阶段进度、可选方案、设计目的、Token 消耗、执行者、推荐模型、操作时间；不适用的项目明确标记为不适用，不为凑齐格式增加无价值步骤。

## 6. 代码与 Notebook 偏好

### Colab 与 Local Notebook

- Cell 1 作为统一 Controller，集中放日期范围、项目、开关、阈值、输入输出位置等人工配置。
- 同一本 Notebook 应同时支持 Colab 与 Local Runtime，业务逻辑保持单一，通过 Secret Loader、Runtime Adapter 和环境配置处理差异。
- 优先使用预配置的非交互式认证，不重复触发授权窗口；如果必须弹窗授权，应紧接 Cell 1 发生，不要运行到中途才弹窗。
- 创建文件、备份或权限确实需要 OAuth 时，应明确说明认证类型和交互要求。
- 主流程应自上而下可运行，不依赖用户在中途手工修改隐藏变量。
- 写入默认采用明确的全量替换逻辑；不随意新建 Tab，同名目标应清空后写入或按已确认规则覆盖。
- 运行结束应输出摘要，至少包含适用时的：`runtime_mode`、`auth_type`、`interactive_auth_used`、`run_id`、`rows_loaded`、`rows_pending`、`rows_planned`、`rows_written`、`warning_count`、输出视图列表。
- 需要保留清晰的顶部版本记录和变更说明。
- 只涉及 1–2 个 Cell 的局部修改时，优先由当前 ChatGPT 直接提供可完整替换的 Cell，不默认调用 Codex；超过 2 个 Cell、跨文件、需要 Git 提交、复杂验证、文档收口或影响范围不确定时，再升级为 Codex 任务。

### 代码

- 代码注释、配置说明、报错解释优先使用中文；API 字段、库名、Sheet 名和专有名词保留原文。
- 代码必须显式失败：关键输入缺失、读取为空、字段不符、写入不完整或回读不一致时，应抛出清楚的异常。
- 不接受静默跳过、吞掉异常后继续、用空结果冒充成功。
- 业务规则应集中定义，避免同一个阈值或字段映射散落在多个 Cell。
- 简化输入表、Controller 或配置 Schema 时，必须把模板、字段常量、读取逻辑、校验、默认值、输出和操作说明作为一次端到端变更；正式交付的新输入应能完整替换旧输入后直接运行，不能把兼容工作留给用户手工删列或补配置。
- Google 写入前优先执行 Dry Run 或 Audit；真实写入前按风险创建备份。已有资产修复优先 targeted repair，只有结构确实不可修复或用户明确授权时才进行 destructive rebuild；这不改变已确认场景中的正式全量覆盖规则。
- 关键写入后应做与风险相称的回读或行数、字段、汇总校验。
- 能由代码可靠维护的运行数据、状态、镜像、目标创建和容量扩充，应由代码自动完成；README、字段字典和设计说明等解释性资产才由人工维护，不能要求用户手工更新程序本可确定的运行结果。

## 7. 文件与版本管理偏好

- 版本化候选可以携带版本号和修复说明；正式 Current 使用稳定、无版本号的操作型名称和固定位置，日常修改更新这个主文件。
- 普通修改历史由 Git History 保存，不为每次修改堆积旧实体副本；只有重大修改、非 Git 资产或旧资产具有独立追溯价值时才保留版本化备份，并放入 Archive，不与 Current 并列充当正式入口。
- 源码权威 Current 与 Runtime 执行副本必须分清：Runtime copy 只服务本地执行或调度，不反向定义 Current；源码或 runner 更新后，需要按规则刷新 Runtime copy。
- 通过 GitHub raw、Notebook/Colab 下载、远程路径或 `importlib` 动态加载的 Python 模块，修改后应默认使用全新版本化文件名；模块内声明可读版本，并在加载或运行入口打印实际文件路径、文件名和版本号，以便直接确认运行的不是缓存或旧模块。
- 本地 Runtime 目录应采用稳定层级命名，例如“用户级 Runtime 根 / 项目或领域 / job_name”的抽象结构，避免散落成临时命名的平行目录；PKB 只记录组织原则，不保存实体绝对路径。
- 不使用 `final_final`、`最新版2` 等无法判断顺序的名称。
- Notebook 常用动词前缀：`Create_`、`Build_`、`Run_`；名称应能直接说明用途。
- 项目专属 Skill、工具和执行入口的名称应明确表达项目与用途，优先清楚、可搜索和可识别，而不是追求抽象或过短。
- Notebook 的内部版本保留在元数据或代码中，不写入正式 Current 文件名。
- 长期文档区分：
  - 方法论/白皮书：跨项目复用的原则、架构、判断标准和常见问题。
  - 项目交接文档：当前真实现场、版本、输入输出、配置、验证结果、故障、限制和下一步。
- 同一主题尽量只有一个明确的 `CURRENT` 文档，旧版用于历史追溯。
- 没有继续使用价值的旧代码或旧产物可以退出 Current，但必须先列出具体候选、用途、删除原因和需迁移知识，并由用户逐项确认；必要时可通过截图或文件列表辅助判断。
- Secret 只记录名称，不记录值。
- 每个本地 Python 项目只使用一套明确的项目专用虚拟环境，并通过依赖清单和安装脚本实现可重建；不提交、压缩或分发整个虚拟环境，Jupyter Kernel 必须指向当前项目解释器。
- Python 虚拟环境与 Secret 配置必须分开管理。Secret 保存在本地中央 Secrets 区域中的项目专用映射里，程序只读取该项目唯一指定的映射，不扫描、猜测或混用其他项目的配置。
- 项目收口先做只读审计，展示完整 Hash、diff、定向变更计划和精确提交范围；确认后才写入或提交。

## 8. 数据与输出原则

- 业务口径先于图表和代码；同名指标必须说明时间范围、粒度和计算方式。
- 唯一键、日期范围、去重规则和汇总粒度必须显式定义。
- 输出应能被经营者直接读懂，并能追溯到来源和运行配置。
- 对需要断点恢复或在线查看的长任务，本地文件应先安全落盘并作为 Source of Truth；在线表格只作为代码维护的镜像，在本地批次成功写入或 Flush 后逐步同步，不能让已取得的数据只停留在内存，也不必等整个长任务结束才发布。
- 默认只把正式业务输出同步到线上；运行日志、失败明细和诊断文件优先保留本地，除非用户明确要求同步。
- 近期广告数据可能重新归因；校验应区分真正错误与可解释的小幅变化，但严格模式必须保留。
- 数据区的格式和颜色用于防止错行、错列和误读；字段列与数值区可以采用不同视觉规则。
- 不因技术便利改变已确认的业务定义。

## 9. 常用技术环境

- Shopify / Shopify GraphQL
- Google Ads API
- GA4 / Search Console / Merchant Center
- Google Sheets / Google Drive / Google Colab
- Python、Pandas、Parquet、CSV、XLSX
- Obsidian / Markdown
- GitHub / VS Code（在适用项目中）

## 10. 尚待我本人补充的信息



## 11. 更新规则

- 只有长期稳定、会影响未来判断的信息才进入本文。
- 临时任务、一次性 ID、短期报错和当天进度不进入本文，应进入对应项目交接文档。
- 新信息与旧信息冲突时，以用户最近一次明确表述为准，并直接更新旧条目。
- 每次重大更新修改“最后更新”日期；必要时在 Git 或外部版本历史中追踪，不在正文堆叠流水账。
- 正式项目完成验收与 Git 收口、且项目工作树干净后，Codex 应主动识别长期有效的用户偏好、协作方式和可复用踩坑，进入本知识库查重、更新必要条目、独立验证并独立提交，不只输出候选建议或等待再次提醒。
- 项目状态与个人长期知识必须分开保存；项目仓库与本知识库不能共享同一个 commit，也不应同时处于未提交状态。
- 本知识库更新严格保留在本地；普通项目版本、commit、资源标识和一次性状态不得写入。
- 已经明确沉淀的长期协作原则应在后续任务中默认执行，不要求用户反复重新说明。
